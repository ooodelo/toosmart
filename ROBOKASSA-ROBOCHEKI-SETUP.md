# ✅ Настройка Робочеков для Robokassa

## Данные организации

**ИП Константинова Софья Сергеевна**
- ИНН: 7710140679
- СНО: УСН доходы (usn_income)
- НДС: Без НДС (none)
- Сайт: https://toosmart.ru

---

## Что такое Робочеки

**Робочеки** - это облачный сервис фискализации от Robokassa.

### Как это работает:

1. **Ваш сайт** → отправляет платеж с параметром `Receipt` в Robokassa
2. **Robokassa** → формирует фискальный чек через свою облачную кассу
3. **Robokassa** → отправляет чек покупателю на email
4. **Robokassa** → отправляет чек в ОФД (оператор фискальных данных)

### Преимущества:
- ✅ Не нужно покупать и настраивать свою ККТ
- ✅ Не нужно интегрироваться с ОФД напрямую
- ✅ Robokassa сама отправляет чеки покупателям
- ✅ Соответствие 54-ФЗ

---

## Что указывается В КОДЕ vs В ЛИЧНОМ КАБИНЕТЕ

### 📋 В личном кабинете Robokassa (один раз при настройке):

**Раздел "Технические настройки" → "Фискализация":**

```
☑️ Включить фискализацию через Робочеки
Тариф: Робочеки

Данные продавца:
- ИНН: 7710140679
- Наименование: ИП Константинова Софья Сергеевна
- Телефон: [ваш телефон]
- Email: [ваш email]
- Адрес расчета: [ваш адрес]

Система налогообложения (СНО): УСН доходы
```

**Эти данные НЕ передаются в коде** - Robokassa автоматически добавляет их в чек из настроек магазина.

---

### 💻 В коде (передается с каждым платежом):

#### Параметр `Receipt` (обязательно):

```json
{
  "sno": "usn_income",
  "items": [
    {
      "name": "Курс \"Теория правильной уборки\"",
      "quantity": 1,
      "sum": "5490.00",
      "tax": "none",
      "payment_method": "full_payment",
      "payment_object": "service"
    }
  ]
}
```

**Обязательные поля в `items`:**

| Поле | Значение | Описание |
|------|----------|----------|
| `name` | "Курс \"Теория правильной уборки\"" | Название товара/услуги (макс. 128 символов) |
| `quantity` | 1 | Количество |
| `sum` | "5490.00" | Полная сумма за всё количество |
| `tax` | "none" | Ставка НДС (none = без НДС) |
| `payment_method` | "full_payment" | Способ расчета (full_payment = полная оплата) |
| `payment_object` | "service" | Предмет расчета (service = услуга) |

#### Параметр `Email` (обязательно):

```php
'Email' => 'user@example.com' // Email покупателя для отправки чека
```

---

## Допустимые значения параметров

### Система налогообложения (sno):

- `osn` — Общая СН
- `usn_income` — УСН доходы ✅ **(используется)**
- `usn_income_outcome` — УСН доходы минус расходы
- `esn` — Единый сельскохозяйственный налог
- `patent` — Патентная СН

### Ставка НДС (tax):

- `none` — Без НДС ✅ **(используется)**
- `vat0` — НДС 0%
- `vat10` — НДС 10%
- `vat20` — НДС 20%
- `vat110` — НДС 10/110
- `vat120` — НДС 20/120

### Способ расчета (payment_method):

- `full_payment` — Полная оплата ✅ **(используется)**
- `full_prepayment` — Предоплата 100%
- `prepayment` — Предоплата (частичная)
- `advance` — Аванс
- `partial_payment` — Частичный расчет и кредит
- `credit` — Передача в кредит
- `credit_payment` — Оплата кредита

### Предмет расчета (payment_object):

- `commodity` — Товар
- `excise` — Подакцизный товар
- `job` — Работа
- `service` — Услуга ✅ **(используется для онлайн-курса)**
- `gambling_bet` — Ставка азартной игры
- `gambling_prize` — Выигрыш азартной игры
- `lottery` — Лотерейный билет
- `lottery_prize` — Выигрыш лотереи
- `intellectual_activity` — Результат интеллектуальной деятельности
- `payment` — Платеж
- `agent_commission` — Агентское вознаграждение
- `composite` — Составной предмет расчета
- `another` — Другое

---

## Как работает код

### 1. Формирование чека (server/api/order/create.php:62-76)

```php
// Формируем чек
$receipt = [
  'sno' => 'usn_income',  // УСН доходы (из настроек)
  'items' => [
    [
      'name' => 'Курс "Теория правильной уборки"',
      'quantity' => 1,
      'sum' => '5490.00',
      'tax' => 'none',
      'payment_method' => 'full_payment',
      'payment_object' => 'service',
    ]
  ]
];

// Конвертируем в JSON
$receipt_json = json_encode($receipt, JSON_UNESCAPED_UNICODE);

// URL-кодируем
$receipt_urlenc = rawurlencode($receipt_json);
```

### 2. Отправка в Robokassa (server/api/order/create.php:111-126)

```php
$params = [
  'MerchantLogin' => 'toosmart',
  'OutSum' => '5490.00',
  'InvId' => 123456,
  'Email' => 'customer@example.com',  // Email покупателя
  'Receipt' => $receipt_urlenc,        // URL-кодированный чек
  // ... другие параметры
];
```

### 3. Robokassa формирует и отправляет чек

После успешной оплаты:
- ✅ Robokassa создает фискальный чек
- ✅ Добавляет данные продавца из ЛК (ИНН, телефон, адрес)
- ✅ Отправляет чек на email покупателя
- ✅ Отправляет чек в ОФД
- ✅ Сохраняет номер фискального документа

---

## ⚠️ Важные моменты

### 1. Receipt НЕ участвует в подписи

Согласно документации Robokassa, параметр `Receipt` **НЕ участвует** в формировании подписи (SignatureValue).

**Формула подписи:**
```
MD5(MerchantLogin:OutSum:InvId:Shp_email=...:Shp_product=...:Password1)
```

Параметр Receipt в эту формулу **НЕ входит**.

Это реализовано в `server/src/robokassa/helpers.php:20-29`:
```php
function rk_make_signature_start(...) {
  // Формула: MD5(MerchantLogin:OutSum:InvId[:Shp_xxx=yyy:...]:Password1)
  // Receipt НЕ участвует в подписи согласно документации Robokassa
  ...
}
```

### 2. Сумма в чеке должна совпадать с OutSum

```php
'OutSum' => '5490.00',  // Сумма платежа

'receipt' => [
  'items' => [
    ['sum' => '5490.00']  // Должна совпадать!
  ]
]
```

Если суммы не совпадают, Robokassa отклонит платеж.

### 3. Метод POST обязателен

Из-за длины параметра `Receipt` (может быть большим JSON), рекомендуется использовать **POST** метод для отправки формы оплаты.

### 4. Лимит товарных позиций

Для Робочеков: **максимум 100 товарных позиций** в одном чеке.

Для вашего курса это не проблема (1 позиция).

---

## 🧪 Тестирование

### Проверьте, что чек формируется правильно:

1. Откройте сайт: https://toosmart.ru
2. Начните оплату курса
3. В консоли браузера (DevTools → Network) проверьте запрос к `order/create.php`
4. Ответ должен содержать:

```json
{
  "endpoint": "https://auth.robokassa.ru/Merchant/Index.aspx",
  "params": {
    "MerchantLogin": "toosmart",
    "OutSum": "5490.00",
    "Receipt": "%7B%22sno%22%3A%22usn_income%22%2C...",
    "Email": "customer@example.com"
  }
}
```

5. Проверьте, что `Receipt` присутствует и закодирован

### После успешной оплаты:

1. На email покупателя должен прийти **фискальный чек** от Robokassa
2. В чеке должно быть:
   - ✅ Название: "Курс "Теория правильной уборки""
   - ✅ Сумма: 5490.00 руб
   - ✅ ИНН продавца: 7710140679
   - ✅ Налог: Без НДС
   - ✅ Номер фискального документа
   - ✅ Фискальный признак документа

---

## 📞 Поддержка

Если возникнут проблемы с Робочеками:

**Robokassa поддержка:**
- Email: support@robokassa.ru
- Телефон: 8 (800) 500-44-55
- Онлайн-чат в ЛК

**Проверка чеков:**
- Проверить чек можно на сайте ФНС: https://kkt-online.nalog.ru/

---

## 📚 Полезные ссылки

- [Фискализация в Robokassa](https://docs.robokassa.ru/fiscalization/)
- [Робочеки - облачные чеки](https://robokassa.com/online-check/robocheck/)
- [Правильное оформление номенклатуры](https://robokassa.com/blog/articles/robocheki-pravilnoe-oformlenie-nomenklatury/)

---

**Дата:** 2025-12-15
**Версия:** 3.0 - Настройка Робочеков
**Статус:** ✅ Настроено для ИП Константинова С.С.
