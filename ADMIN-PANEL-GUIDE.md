# 🖥️ Админ-панель: Управление Legal документами

## Текущий функционал

### Что работает в админ-панели

В админ-панели (`http://localhost:3001`) есть секция **"📄 Legal документы"** с **3 текстовыми полями**:

```html
┌──────────────────────────────────┐
│    📄 Legal документы            │
├──────────────────────────────────┤
│ Файл условий/оферты              │
│ [01-legal-terms.md            ]  │ ← поле для ввода
│ Имя файла в content/legal/       │
│                                  │
│ Политика конфиденциальности      │
│ [02-privacy-policy.md         ]  │ ← поле для ввода
│                                  │
│ Публичная оферта                 │
│ [03-public-offer.md           ]  │ ← поле для ввода
│                                  │
│ [💾 Сохранить конфигурацию]     │ ← общая кнопка сохранения
└──────────────────────────────────┘
```

### Как это работает

1. **Вы указываете имя файла** в текстовом поле (например: `01-legal-terms.md`)
2. **Нажимаете "💾 Сохранить конфигурацию"**
3. **Имя сохраняется в `/config/site.json`** в секции `"legal"`
4. **При сборке проекта** используется это имя для подгрузки файла

Конфиг выглядит так:

```json
{
  "legal": {
    "terms": "01-legal-terms.md",
    "privacy": "02-privacy-policy.md",
    "offer": "03-public-offer.md"
  }
}
```

---

## ⚠️ ВАЖНО: Загрузка файлов НЕ реализована

### Что пока НЕ работает

В админ-панели **отсутствует функция загрузки файлов**.

**Это означает:**
- ❌ Нельзя загружать `.md` файлы через админ-панель
- ❌ Нельзя загружать другие форматы (PDF, DOCX и т.д.)
- ✅ Можно только указывать имена файлов, которые уже существуют в папке

### Почему так?

В сервере админ-панели (`/admin/server.js`, строка 242-247) функция загрузки есть, но не реализована:

```javascript
// Загрузка файлов
async function handleFileUpload(req, res) {
  // Simplified file upload handler
  // In production, use proper multipart/form-data parser
  res.writeHead(501, { 'Content-Type': 'application/json' });
  res.end(JSON.stringify({ error: 'Загрузка файлов пока не реализована' }));
}
```

Статус 501 означает «Not Implemented» (не реализовано).

---

## 📂 Текущий рабочий процесс

### Шаг 1: Создайте файл вручную

Создайте `.md` файл в папке `/content/legal/`:

```bash
cat > /home/user/toosmart/content/legal/04-refund-policy.md << 'EOF'
# Политика возврата

Описание политики возврата...
EOF
```

### Шаг 2: Откройте админ-панель

```bash
npm run admin
# Откроется http://localhost:3001
```

### Шаг 3: Укажите имя файла

В админ-панели найдите секцию "Legal документы" и введите имя файла:

```
04-refund-policy.md
```

### Шаг 4: Сохраните конфигурацию

Нажмите кнопку **"💾 Сохранить конфигурацию"** в конце страницы.

### Шаг 5: Соберите проект

Нажмите **"🔄 Собрать всё"** или другую кнопку сборки.

---

## 🚀 Возможные улучшения (в будущем)

Если нужна функция загрузки файлов, можно добавить:

### Вариант 1: Загрузка через админ-панель (рекомендуется)

**Что добавить:**
- Кнопка "Выбрать файл" для загрузки `.md` файлов
- Обработчик на сервере для сохранения файлов в `/content/legal/`
- Валидация (проверка формата, размера)
- Автоматическое переименование файла (добавление номера)

**Код для реализации:**
```html
<!-- Добавить в админ-панель -->
<div class="form-group">
  <label for="legalTermsFile">Загрузить файл условий</label>
  <input type="file" id="legalTermsFile" accept=".md" />
  <button onclick="uploadLegalFile('terms')">Загрузить</button>
</div>
```

### Вариант 2: Прямое редактирование через админ-панель

**Что добавить:**
- Textarea с возможностью редактирования содержимого файла
- Кнопка "Сохранить содержимое"
- Возможность просмотра предпросмотра

**Преимущества:**
- Не нужно работать с файлами через terminal
- Все в одном месте
- Версионирование через бэкапы

### Вариант 3: Файловый менеджер в админ-панели

**Что добавить:**
- Список всех файлов в `/content/legal/`
- Возможность загрузки новых файлов
- Возможность удаления/переименования файлов
- Редактор содержимого прямо в админ-панели

---

## 🔧 Текущая структура в коде

### Админ-панель (`/admin/index.html`)

**Строки 659-674** — HTML форма для legal документов:

```html
<div class="card">
  <h2><span class="icon">📄</span> Legal документы</h2>
  <div class="form-group">
    <label for="legalTerms">Файл условий/оферты</label>
    <input type="text" id="legalTerms" placeholder="01-legal-terms.md">
    <div class="hint">Имя файла в content/legal/</div>
  </div>
  <div class="form-group">
    <label for="legalPrivacy">Политика конфиденциальности</label>
    <input type="text" id="legalPrivacy" placeholder="02-privacy.md">
  </div>
  <div class="form-group">
    <label for="legalOffer">Публичная оферта</label>
    <input type="text" id="legalOffer" placeholder="03-offer.md">
  </div>
</div>
```

Эти поля **автоматически заполняются** функцией `populateForm()` из конфига и **сохраняются** функцией `collectFormData()`.

### Сервер админ-панели (`/admin/server.js`)

**Строки 60-66** — обработчик запроса `/api/config`:

```javascript
if (pathname === '/api/config') {
  if (req.method === 'GET') {
    await handleGetConfig(req, res);  // Получить конфиг
  } else if (req.method === 'POST') {
    await handleSaveConfig(req, res); // Сохранить конфиг
  }
  return;
}
```

**Строки 74-77** — (не реализованный) обработчик загрузки файлов:

```javascript
if (pathname === '/api/files' && req.method === 'POST') {
  await handleFileUpload(req, res);
  return;
}
```

### При сборке (`/scripts/build.js`)

При выполнении сборки скрипт:
1. Читает конфиг: `config.legal.*`
2. Проверяет наличие файлов в `/content/legal/`
3. Читает содержимое markdown файлов
4. Преобразует markdown в HTML
5. Подключает в финальный результат

---

## ✅ Best Practices

### Как правильно работать с legal документами

1. **Используйте папку `/content/legal/`** для хранения файлов
2. **Называйте файлы систематично** (01-, 02-, 03- и т.д.)
3. **Создавайте файлы вручную** или через terminal
4. **Указывайте имена в админ-панели** в текстовых полях
5. **Сохраняйте конфиг** перед сборкой
6. **Проверяйте логи сборки** на ошибки

### Чего избегать

- ❌ Не загружайте файлы в неправильную папку
- ❌ Не забывайте расширение `.md`
- ❌ Не используйте кириллицу в именах файлов
- ❌ Не оставляйте поля пустыми (указывайте имена)

---

## 📋 Чек-лист для нового legal документа

Если вы добавляете новый документ:

- [ ] Создан файл в `/content/legal/NN-name.md`
- [ ] Файл назван правильно (с номером и расширением)
- [ ] Содержимое написано на markdown
- [ ] Указано имя в админ-панели (если это поле есть)
- [ ] Конфиг сохранен через админ-панель
- [ ] Проект собран успешно
- [ ] В `/dist/` файл правильно подключен

---

## 📞 Поддержка

Если нужна функция загрузки файлов — создайте задачу для разработки.

До тех пор используйте текущий способ:
1. Создайте файл вручную
2. Укажите имя в админ-панели
3. Соберите проект

