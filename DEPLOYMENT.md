# 🚀 ПОЛНАЯ ИНСТРУКЦИЯ ПО РАЗВЕРТЫВАНИЮ НА ХОСТИНГЕ

**Версия:** 1.1
**Дата:** 2025-11-23
**Проект:** TooSmart - Образовательная платформа с Robokassa

---

## 📌 CHANGELOG

### Версия 1.1 (2025-11-23)

**Изменения в структуре:**
- ✅ Упрощена структура dist - бесплатный контент теперь в корне (course/, legal/, recommendations/)
- ✅ Добавлен sitemap.xml для SEO
- ✅ Добавлена страница fail.php для неуспешных оплат
- ✅ Добавлены файлы для восстановления пароля (опционально)

**Новые функции:**
- Восстановление пароля через email (forgot-password.php, reset-password.php)
- Смена пароля в личном кабинете (change-password.php)
- Страница настроек пользователя (settings.html)
- Кнопка "Выйти" в меню премиум версии

**База данных:**
- Добавлена опциональная таблица `password_reset_tokens` для функционала восстановления пароля

**ВАЖНО:** Файлы forgot-password.php, reset-password.php, change-password.php, settings.html НЕ копируются автоматически при сборке. Если вам нужны эти функции, скопируйте их вручную из папки `server/` на сервер и примените миграцию `add_password_reset_tokens.sql`.

---

## 📋 СОДЕРЖАНИЕ

1. [Changelog](#changelog)
2. [Что нужно знать перед началом](#что-нужно-знать-перед-началом)
3. [Требования к хостингу](#требования-к-хостингу)
4. [Процесс сборки проекта](#процесс-сборки-проекта)
5. [Структура dist папки](#структура-dist-папки)
6. [Что загружать на хостинг](#что-загружать-на-хостинг)
7. [Настройка сервера](#настройка-сервера)
8. [Настройка MySQL базы данных](#настройка-mysql-базы-данных)
9. [Настройка конфигурационных файлов](#настройка-конфигурационных-файлов)
10. [Настройка Robokassa](#настройка-robokassa)
11. [Настройка email](#настройка-email)
12. [Проверка работоспособности](#проверка-работоспособности)
13. [Финальный чеклист](#финальный-чеклист)
14. [Дополнительные функции (опционально)](#дополнительные-функции-опционально)
15. [Troubleshooting](#troubleshooting)

---

## ⚠️ ЧТО НУЖНО ЗНАТЬ ПЕРЕД НАЧАЛОМ

### Архитектура проекта

Проект состоит из двух частей:

1. **FRONTEND** (статические HTML/CSS/JS файлы)
   - Бесплатная версия курса (в корне сайта)
   - Премиум версия курса (защищенная PHP аутентификацией)
   - Рекомендательные статьи
   - Юридические документы

2. **BACKEND** (PHP + MySQL)
   - Система оплаты через Robokassa
   - Аутентификация пользователей (Magic Links)
   - Управление доступом к премиум контенту
   - Фискализация чеков (54-ФЗ)

---

## 💻 ТРЕБОВАНИЯ К ХОСТИНГУ

### Обязательные требования:

**Для frontend:**
- Веб-сервер (Apache/Nginx)
- Поддержка HTTPS (обязательно для оплаты!)
- Возможность настройки .htaccess (для Apache)

**Для backend:**
- PHP 7.4+ (рекомендуется PHP 8.0+)
- MySQL 5.7+ или MariaDB 10.2+
- PHP модули:
  - `pdo_mysql` (для работы с БД)
  - `mbstring` (для работы с кириллицей)
  - `json` (для обработки JSON)
  - `openssl` (для генерации токенов)
- Доступ к MySQL через phpMyAdmin или командную строку
- Возможность создавать папки вне public_html (для приватных данных)

**Для email:**
- Настроенный sendmail/phpmail ИЛИ доступ к SMTP серверу

### Проверка PHP модулей на хостинге:

Создайте файл `check.php` и загрузите на хостинг:
```php
<?php
phpinfo();
?>
```

Откройте в браузере и проверьте наличие всех модулей.

---

## 🔨 ПРОЦЕСС СБОРКИ ПРОЕКТА

### Локально перед развертыванием:

```bash
# 1. Установите зависимости (если еще не установлены)
npm install

# 2. Очистите предыдущую сборку
npm run clean

# 3. Выполните полную сборку
npm run build
```

Команда `npm run build` выполняет:
- Компиляцию CSS/JS через Vite
- Генерацию HTML страниц из Markdown
- Копирование серверных файлов
- Оптимизацию ресурсов

### Результат сборки:

После успешной сборки появится папка `dist/` с готовыми файлами для развертывания.

---

## 📁 СТРУКТУРА DIST ПАПКИ

После выполнения `npm run build` структура будет следующей:

```
dist/
├── index.html                    # Главная страница (intro)
├── robots.txt                    # Файл для поисковиков
├── sitemap.xml                   # Карта сайта для поисковиков
├── course/                       # Бесплатные статьи курса (заблюренные)
│   ├── раздел-1-основы.html
│   ├── раздел-2-инструменты.html
│   └── ... (все 10 разделов)
├── legal/                        # Юридические документы
│   ├── legal-terms.html          # Условия использования
│   ├── privacy-policy.html       # Политика конфиденциальности
│   └── public-offer.html         # Публичная оферта
├── recommendations/              # Рекомендательные статьи
│   ├── eco-cleaning.html
│   └── ...
├── assets/                       # Статические ресурсы
│   ├── index-[hash].js           # JS файлы (хешированные)
│   ├── index-[hash].css          # CSS файлы (хешированные)
│   ├── favicon.svg               # Иконка сайта
│   ├── content/                  # Изображения из контента
│   └── .vite/                    # Manifest файлы Vite
├── shared/                       # Общие данные
│   ├── recommendations.json      # Список рекомендаций
│   └── site.json                 # Конфигурация сайта
└── premium/                      # ПРЕМИУМ ВЕРСИЯ (защищенная)
    ├── index.html                # Вход в премиум (intro)
    ├── .htaccess                 # Правила защиты доступа
    ├── course/                   # Все статьи курса (полные версии)
    │   ├── раздел-1-основы.html
    │   └── ... (все 10 разделов)
    ├── appendix/                 # Приложения
    │   └── ...
    ├── api/                      # API endpoints
    │   ├── auth/
    │   │   ├── login.php
    │   │   ├── magic-consume.php
    │   │   └── set-password.php
    │   └── order/
    │       └── create.php
    ├── robokassa/
    │   └── result.php            # ResultURL для Robokassa
    ├── src/                      # Исходники PHP модулей
    │   ├── auth/
    │   ├── order/
    │   ├── robokassa/
    │   ├── admin/
    │   ├── utils.php
    │   ├── config_loader.php
    │   ├── schema_init.php
    │   └── mailer.php
    ├── sql/
    │   ├── schema.sql            # SQL схема для MySQL
    │   └── add_password_reset_tokens.sql  # Миграция для восстановления пароля
    ├── storage/                  # Конфигурация (создается вручную)
    │   ├── settings.json         # Конфигурация (создается вручную на сервере)
    │   ├── products.json         # Продукты (создается вручную на сервере)
    │   └── *.example             # Примеры конфигураций
    ├── auth.php                  # Вход по паролю
    ├── check-auth.php            # Проверка доступа
    ├── logout.php                # Выход
    ├── success.php               # Страница успешной оплаты
    ├── fail.php                  # Страница неуспешной оплаты
    ├── health.php                # Health check
    ├── config.php                # Загрузчик конфигурации (legacy)
    ├── Database.php              # Работа с БД (legacy)
    ├── security.php              # Функции безопасности (legacy)
    ├── robokassa-callback.php    # Обработчик оплаты (legacy)
    ├── create-invoice.php        # Создание счета (legacy)
    └── users.json.example        # Пример файла пользователей (legacy)
```

**Примечание:** Файлы для восстановления/смены пароля (forgot-password.php, reset-password.php, change-password.php, settings.html) находятся в исходниках `server/`, но НЕ копируются автоматически в `dist/` при сборке. Если вам нужны эти функции, скопируйте их вручную на сервер.

---

## 📤 ЧТО ЗАГРУЖАТЬ НА ХОСТИНГ

### ВАЖНО: Структура размещения на сервере

**НА ХОСТИНГЕ ДОЛЖНА БЫТЬ СЛЕДУЮЩАЯ СТРУКТУРА:**

```
/                                 # Корень вашего домена (обычно public_html/)
├── index.html                    # Главная страница
├── robots.txt
├── course/                       # Бесплатные статьи
├── legal/                        # Юридические документы
├── recommendations/              # Рекомендации
├── assets/                       # Статические ресурсы
├── shared/                       # JSON данные
└── premium/                      # Папка премиум (вся целиком!)
    ├── .htaccess
    ├── index.html
    ├── *.php файлы
    ├── course/
    ├── appendix/
    ├── api/
    ├── robokassa/
    ├── src/
    ├── sql/
    └── storage/                  # Создается вручную на сервере!
```

### Пошаговая загрузка:

#### 1. Загрузите содержимое dist/ в корень сайта

Через FTP/SFTP или панель хостинга загрузите **ВЕСЬ** каталог `dist/` на сервер:

- **Откуда:** `dist/*` (все содержимое папки dist)
- **Куда:** Корень вашего сайта (`public_html/` или `/var/www/html/`)

**Способ 1: FTP клиент (FileZilla)**
1. Подключитесь к хостингу через FTP
2. Перейдите в `public_html/`
3. Перетащите всё содержимое из `dist/` в `public_html/`

**Способ 2: SSH + rsync (если есть доступ)**
```bash
# С вашего компьютера
rsync -avz --delete dist/ user@yourhost.com:/var/www/html/
```

**Способ 3: Панель хостинга**
1. Создайте ZIP архив из папки dist
2. Загрузите через веб-интерфейс хостинга
3. Распакуйте в корень сайта

#### 2. КРИТИЧЕСКИ ВАЖНО: Не загружайте в подпапку!

❌ **НЕПРАВИЛЬНО:**
```
public_html/
└── dist/                         # НЕ ТАК!
    ├── index.html
    └── ...
```

✅ **ПРАВИЛЬНО:**
```
public_html/
├── index.html                    # Все файлы в корне!
├── course/
├── premium/
└── ...
```

---

## ⚙️ НАСТРОЙКА СЕРВЕРА

### 1. Создайте папку для приватных данных

**ВАЖНО:** Конфигурационные файлы с паролями НЕ должны быть в public_html!

Создайте папку **вне** веб-корня:

```bash
# Если у вас есть SSH доступ:
mkdir -p /home/youruser/private
chmod 700 /home/youruser/private

# Альтернатива (если SSH нет):
# Создайте папку через панель хостинга на уровень выше public_html/
```

Структура должна быть:
```
/home/youruser/
├── public_html/          # Веб-корень
│   ├── index.html
│   └── premium/
└── private/              # ПРИВАТНАЯ папка
    ├── database.sqlite   # База данных (если используется SQLite)
    └── users.json        # Данные пользователей (legacy)
```

### 2. Настройте права доступа

```bash
# Для premium папки
chmod 755 /path/to/public_html/premium/
chmod 644 /path/to/public_html/premium/*.php
chmod 644 /path/to/public_html/premium/.htaccess

# Для storage (конфигурация)
chmod 700 /path/to/public_html/premium/storage/
chmod 600 /path/to/public_html/premium/storage/*.json

# Для приватной папки
chmod 700 /home/youruser/private/
chmod 600 /home/youruser/private/*
```

### 3. Проверьте .htaccess в premium/

Файл `premium/.htaccess` уже создан при сборке. Убедитесь что он загружен!

**Что он делает:**
- Защищает HTML/CSS/JS от прямого доступа
- Перенаправляет все .html запросы через `check-auth.php`
- Добавляет заголовки безопасности
- Блокирует скраперов

**ВАЖНО:** Измените домен в .htaccess:
```apache
# Найдите строку:
RewriteCond %{HTTP_REFERER} !^https://(www\.)?toosmart\.com [NC]

# Замените на ваш домен:
RewriteCond %{HTTP_REFERER} !^https://(www\.)?yourdomain\.com [NC]
```

---

## 🗄️ НАСТРОЙКА MYSQL БАЗЫ ДАННЫХ

### Шаг 1: Создайте базу данных

Через phpMyAdmin или панель хостинга:

1. Создайте новую БД:
   - Имя: `toosmart` (или любое другое)
   - Кодировка: `utf8mb4_unicode_ci`

**SQL команда:**
```sql
CREATE DATABASE toosmart CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### Шаг 2: Создайте пользователя БД

```sql
CREATE USER 'toosmart_user'@'localhost' IDENTIFIED BY 'НАДЕЖНЫЙ_ПАРОЛЬ_123';
GRANT ALL PRIVILEGES ON toosmart.* TO 'toosmart_user'@'localhost';
FLUSH PRIVILEGES;
```

**Сохраните:**
- Имя базы: `toosmart`
- Пользователь: `toosmart_user`
- Пароль: `НАДЕЖНЫЙ_ПАРОЛЬ_123`

### Шаг 3: Импортируйте схему

1. Откройте phpMyAdmin
2. Выберите базу `toosmart`
3. Перейдите на вкладку "Import" / "Импорт"
4. Выберите файл `premium/sql/schema.sql`
5. Нажмите "Выполнить" / "Go"

**Или через SSH:**
```bash
mysql -u toosmart_user -p toosmart < /path/to/public_html/premium/sql/schema.sql
```

### Шаг 4: Проверьте таблицы

Выполните в phpMyAdmin:
```sql
USE toosmart;
SHOW TABLES;
```

Должны быть созданы:
- `users` - пользователи
- `orders` - заказы
- `access` - доступы к премиум
- `magic_links` - токены для входа

### Шаг 5 (опционально): Добавьте таблицу для восстановления пароля

Если вы скопировали файлы forgot-password.php и reset-password.php на сервер, нужно создать дополнительную таблицу:

1. Откройте phpMyAdmin
2. Выберите базу `toosmart`
3. Перейдите на вкладку "Import" / "Импорт"
4. Выберите файл `premium/sql/add_password_reset_tokens.sql`
5. Нажмите "Выполнить" / "Go"

**Или через SSH:**
```bash
mysql -u toosmart_user -p toosmart < /path/to/public_html/premium/sql/add_password_reset_tokens.sql
```

Это создаст таблицу `password_reset_tokens` для функционала восстановления пароля.

---

## 📝 НАСТРОЙКА КОНФИГУРАЦИОННЫХ ФАЙЛОВ

### Шаг 1: Создайте папку storage на сервере

```bash
# В папке premium создайте storage
mkdir /path/to/public_html/premium/storage
chmod 700 /path/to/public_html/premium/storage
```

### Шаг 2: Создайте settings.json

На сервере создайте файл `premium/storage/settings.json`:

```json
{
  "db": {
    "dsn": "mysql:host=localhost;dbname=toosmart;charset=utf8mb4",
    "user": "toosmart_user",
    "pass": "НАДЕЖНЫЙ_ПАРОЛЬ_123"
  },
  "robokassa": {
    "merchant_login": "ВАШ_ЛОГИН_ИЗ_ROBOKASSA",
    "pass1": "ПАРОЛЬ_1_ИЗ_ROBOKASSA",
    "pass2": "ПАРОЛЬ_2_ИЗ_ROBOKASSA",
    "is_test": false,
    "signature_alg": "md5",
    "success_url": "https://yourdomain.com/premium/success.php",
    "fail_url": "https://yourdomain.com/premium/fail.php",
    "default_sno": "usn_income",
    "default_tax": "none",
    "product_code": "premium_course"
  },
  "security": {
    "session_name": "toosmart_premium",
    "admin_token": "СГЕНЕРИРУЙТЕ_СЛУЧАЙНУЮ_СТРОКУ_МИНИМУМ_32_СИМВОЛА"
  },
  "emails": {
    "from": "noreply@yourdomain.com",
    "transport": "phpmail"
  },
  "site": {
    "base_url": "https://yourdomain.com"
  }
}
```

**Генерация admin_token:**
```bash
# На вашем компьютере:
openssl rand -hex 32

# Или онлайн: https://www.random.org/strings/
```

**Установите права:**
```bash
chmod 600 /path/to/public_html/premium/storage/settings.json
```

### Шаг 3: Создайте products.json

Файл уже есть в `premium/storage/products.json.example`. Скопируйте его:

```bash
cp premium/storage/products.json.example premium/storage/products.json
```

Содержимое (можно изменить цену):
```json
{
  "premium_course": {
    "name": "Курс \"Clean - Теория правильной уборки\"",
    "price": 990.0,
    "tax": "none",
    "payment_method": "full_payment",
    "payment_object": "service"
  }
}
```

**Установите права:**
```bash
chmod 600 /path/to/public_html/premium/storage/products.json
```

---

## 💳 НАСТРОЙКА ROBOKASSA

### Шаг 1: Регистрация в Robokassa

1. Зарегистрируйтесь на https://auth.robokassa.ru/
2. Создайте новый магазин
3. Заполните данные о вашей компании

### Шаг 2: Технические настройки магазина

В личном кабинете Robokassa:

**Раздел "Технические настройки":**

1. **Алгоритм подписи:** MD5 (или SHA256)
2. **Фискализация:**
   - ✅ Включите "Отправлять чеки в ОФД" (обязательно для 54-ФЗ!)
3. **URL адреса:**
   - **Result URL:** `https://yourdomain.com/premium/robokassa/result.php`
   - **Success URL:** `https://yourdomain.com/premium/success.php`
   - **Fail URL:** `https://yourdomain.com/premium/fail.php`
4. **Метод отправки данных:**
   - Result URL: `POST`
   - Success/Fail: `GET` или `POST` (любой)

### Шаг 3: Получите пароли

В ЛК Robokassa скопируйте:
- **Merchant Login** (идентификатор магазина)
- **Password #1** (для генерации ссылки на оплату)
- **Password #2** (для проверки Result URL)

**Вставьте их в `premium/storage/settings.json`!**

### Шаг 4: Тестовый режим

Для тестирования:
1. В ЛК Robokassa включите "Тестовый режим"
2. В `settings.json` установите `"is_test": true`
3. Используйте **тестовые** пароли из ЛК

Для продакшена:
1. В ЛК Robokassa отключите "Тестовый режим"
2. В `settings.json` установите `"is_test": false`
3. Используйте **боевые** пароли

### Шаг 5: Проверьте доступность Result URL

**КРИТИЧЕСКИ ВАЖНО:** Robokassa должна иметь возможность вызвать Result URL!

Проверьте:
```bash
curl -X POST https://yourdomain.com/premium/robokassa/result.php
```

Должен вернуться код 200 (даже если ошибка валидации).

Если 403/404/500 - проверьте:
- Файл существует и загружен
- Права доступа настроены
- .htaccess не блокирует POST запросы
- Firewall хостинга не блокирует IP Robokassa

---

## ✉️ НАСТРОЙКА EMAIL

Проект отправляет email для:
- Magic links (ссылки для входа)
- Уведомления об оплате

### Вариант 1: PHP Mail (простой, но может не работать)

В `settings.json`:
```json
"emails": {
  "from": "noreply@yourdomain.com",
  "transport": "phpmail"
}
```

**Требования:**
- На сервере настроен sendmail
- Хостинг не блокирует отправку почты

**Проверка:**
```bash
# На сервере
echo "Test" | mail -s "Test" your@email.com
```

### Вариант 2: SMTP (рекомендуется!)

Используйте внешний SMTP сервер (Gmail, Yandex, SendGrid, etc):

```json
"emails": {
  "from": "noreply@yourdomain.com",
  "transport": "smtp",
  "smtp": {
    "host": "smtp.gmail.com",
    "port": 587,
    "user": "your@gmail.com",
    "pass": "APP_PASSWORD_HERE",
    "secure": "tls"
  }
}
```

**Для Gmail:**
1. Включите 2FA в аккаунте Google
2. Создайте "App Password" на https://myaccount.google.com/apppasswords
3. Используйте этот пароль в `smtp.pass`

**Для Yandex:**
```json
"smtp": {
  "host": "smtp.yandex.ru",
  "port": 465,
  "user": "your@yandex.ru",
  "pass": "PASSWORD",
  "secure": "ssl"
}
```

---

## ✅ ПРОВЕРКА РАБОТОСПОСОБНОСТИ

### 1. Health Check

Откройте в браузере:
```
https://yourdomain.com/premium/health.php
```

**Ожидаемый ответ:**
```json
{
  "status": "ok",
  "database": "connected",
  "users_count": 0,
  "timestamp": "2025-11-22 12:00:00"
}
```

Если ошибка:
- Проверьте `settings.json` (правильность данных БД)
- Проверьте подключение к MySQL
- Посмотрите логи PHP на сервере

### 2. Проверьте главную страницу

```
https://yourdomain.com/
```

Должна открыться главная страница курса (intro).

### 3. Проверьте бесплатные статьи

```
https://yourdomain.com/course/раздел-1-основы.html
```

Должна открыться статья с заблюренным контентом и кнопкой "Войти в полную версию".

### 4. Проверьте модальное окно оплаты

Нажмите кнопку CTA → должно открыться модальное окно с формой:
- Поле для email
- Чекбокс согласия с офертой
- Кнопка "Оплатить 990 ₽"

### 5. Тестовая оплата

1. Введите ваш email
2. Согласитесь с офертой
3. Нажмите "Оплатить"
4. Должен произойти редирект на страницу Robokassa

**В тестовом режиме:**
- Используйте тестовую карту: `5555 5555 5555 5599`
- CVV: любой
- Срок: любой будущий

5. Подтвердите оплату
6. Robokassa вызовет Result URL
7. На ваш email придет письмо с magic link

### 6. Проверьте Magic Link

1. Откройте письмо
2. Перейдите по ссылке вида:
   ```
   https://yourdomain.com/premium/api/auth/magic-consume.php?token=...&email=...
   ```
3. Должен произойти автоматический вход
4. Редирект на `/premium/`
5. Вы должны увидеть полную версию курса

### 7. Проверьте доступ к премиум контенту

Перейдите на:
```
https://yourdomain.com/premium/course/раздел-1-основы.html
```

Должна открыться полная статья без ограничений.

---

## 📋 ФИНАЛЬНЫЙ ЧЕКЛИСТ

Перед переходом в продакшен убедитесь:

### Хостинг и сервер:
- [ ] Все файлы из `dist/` загружены в корень сайта
- [ ] Структура папок корректная (premium в корне, не в подпапке)
- [ ] Права доступа настроены (755 для папок, 644 для файлов)
- [ ] .htaccess файлы загружены и работают
- [ ] HTTPS настроен и работает (обязательно!)

### База данных:
- [ ] MySQL база создана
- [ ] Пользователь БД создан с правильными правами
- [ ] Схема импортирована (4 таблицы созданы)
- [ ] Подключение к БД работает (health check успешен)

### Конфигурация:
- [ ] `premium/storage/settings.json` создан
- [ ] Данные MySQL правильные
- [ ] Robokassa credentials вставлены
- [ ] admin_token сгенерирован (минимум 32 символа)
- [ ] Домен указан правильно (`site.base_url`)
- [ ] Права на файлы: 600 (только владелец может читать)

### Robokassa:
- [ ] Магазин создан и настроен
- [ ] Фискализация включена (54-ФЗ)
- [ ] Result URL указан: `https://yourdomain.com/premium/robokassa/result.php`
- [ ] Success URL указан: `https://yourdomain.com/premium/success.php`
- [ ] Fail URL указан: `https://yourdomain.com/premium/fail.php`
- [ ] Result URL доступен извне (не возвращает 403/404)
- [ ] Тестовый режим: `"is_test": false` для продакшена
- [ ] Используются **боевые** пароли (не тестовые!)

### Email:
- [ ] Email настроен (phpmail или SMTP)
- [ ] Тестовое письмо отправлено успешно
- [ ] Magic links приходят на почту

### Тестирование:
- [ ] Health check возвращает `"status": "ok"`
- [ ] Главная страница открывается
- [ ] Бесплатные статьи открываются
- [ ] Модальное окно оплаты работает
- [ ] Тестовая оплата прошла успешно
- [ ] Magic link работает и дает доступ
- [ ] Премиум контент доступен после входа
- [ ] Выход работает (logout.php)

### Безопасность:
- [ ] settings.json имеет права 600
- [ ] products.json имеет права 600
- [ ] Приватные данные не в public_html
- [ ] admin_token сложный и случайный
- [ ] Пароли от MySQL надежные
- [ ] HTTPS работает на всех страницах

### Юридические документы:
- [ ] Условия использования отредактированы
- [ ] Политика конфиденциальности отредактирована
- [ ] Публичная оферта отредактирована
- [ ] Все плейсхолдеры заменены (example.com → yourdomain.com)
- [ ] ИНН и адрес компании указаны

---

## 🎁 ДОПОЛНИТЕЛЬНЫЕ ФУНКЦИИ (ОПЦИОНАЛЬНО)

Эти функции не включены в автоматическую сборку, но доступны в исходниках и могут быть добавлены вручную.

### Восстановление пароля через email

**Что это дает:**
- Пользователи могут восстанавливать пароль если забыли
- Отправка токена восстановления на email
- Безопасный процесс смены пароля

**Как включить:**

1. **Скопируйте файлы на сервер:**
   ```bash
   # Из локальной папки server/ скопируйте на хостинг в premium/:
   - forgot-password.html      → premium/forgot-password.html
   - forgot-password.php       → premium/forgot-password.php
   - reset-password-form.html  → premium/reset-password-form.html
   - reset-password.php        → premium/reset-password.php
   - resend-password.html      → premium/resend-password.html
   - resend-password.php       → premium/resend-password.php
   ```

2. **Примените SQL миграцию:**
   ```bash
   mysql -u toosmart_user -p toosmart < premium/sql/add_password_reset_tokens.sql
   ```

   Это создаст таблицу `password_reset_tokens`.

3. **Проверьте работу:**
   - Откройте `https://yourdomain.com/premium/forgot-password.html`
   - Введите email зарегистрированного пользователя
   - Проверьте что письмо с токеном пришло
   - Перейдите по ссылке и смените пароль

### Смена пароля в личном кабинете

**Что это дает:**
- Пользователи могут изменить пароль находясь в премиум версии
- Повышение безопасности учетной записи

**Как включить:**

1. **Скопируйте файлы на сервер:**
   ```bash
   # Из локальной папки server/ скопируйте на хостинг в premium/:
   - change-password.php  → premium/change-password.php
   - settings.html        → premium/settings.html
   ```

2. **Добавьте ссылку в меню** (опционально):

   Отредактируйте файл шаблона или добавьте ссылку вручную в `premium/index.html`:
   ```html
   <a href="/premium/settings.html">Настройки</a>
   ```

3. **Проверьте работу:**
   - Войдите в премиум версию
   - Откройте `https://yourdomain.com/premium/settings.html`
   - Измените пароль

### Важные замечания

⚠️ **Для работы этих функций необходимо:**
- Настроенная отправка email (SMTP или phpmail)
- Таблица `password_reset_tokens` в базе данных
- Токены восстановления действуют 1 час
- После использования токен удаляется

⚠️ **Безопасность:**
- Убедитесь что HTTPS работает (обязательно!)
- Проверьте права доступа к файлам (644 для PHP файлов)
- Используйте сложные пароли для БД

---

## 🔧 TROUBLESHOOTING

### Проблема: Health check возвращает ошибку

**Симптомы:**
- HTTP 500
- Пустая страница
- Ошибка "Database connection failed"

**Решение:**
1. Проверьте `settings.json`:
   - DSN правильный (`mysql:host=localhost;dbname=toosmart`)
   - Имя пользователя и пароль верные
2. Проверьте что MySQL база создана
3. Проверьте что пользователь имеет права на БД:
   ```sql
   SHOW GRANTS FOR 'toosmart_user'@'localhost';
   ```
4. Проверьте логи PHP:
   - cPanel: Error Logs
   - SSH: `tail -f /var/log/php-errors.log`

### Проблема: Robokassa не вызывает Result URL

**Симптомы:**
- Оплата проходит, но письмо не приходит
- Заказ не создается в БД
- В логах Robokassa: "Result URL недоступен"

**Решение:**
1. Проверьте что URL доступен извне:
   ```bash
   curl -X POST https://yourdomain.com/premium/robokassa/result.php
   ```
2. Проверьте .htaccess - не блокирует ли POST запросы
3. Проверьте firewall хостинга - добавьте IP Robokassa в whitelist
4. Проверьте что URL указан правильно в ЛК Robokassa
5. Проверьте логи Apache/Nginx

### Проблема: Invalid signature от Robokassa

**Симптомы:**
- Ошибка "Invalid signature" в логах
- Result URL вызывается, но заказ не создается

**Решение:**
1. Проверьте что `pass1` и `pass2` совпадают с ЛК Robokassa
2. Убедитесь что используете правильные пароли:
   - Тестовый режим = тестовые пароли
   - Боевой режим = боевые пароли
3. Проверьте `signature_alg` в settings.json (должен совпадать с ЛК)
4. Проверьте что `merchant_login` правильный

### Проблема: Email не отправляются

**Симптомы:**
- Magic link не приходит
- В БД заказ создан, но письма нет

**Решение:**

**Если используете phpmail:**
1. Проверьте что sendmail настроен на сервере:
   ```bash
   which sendmail
   ```
2. Проверьте что хостинг не блокирует отправку почты
3. Переключитесь на SMTP (рекомендуется)

**Если используете SMTP:**
1. Проверьте данные SMTP в `settings.json`
2. Проверьте что порт открыт:
   ```bash
   telnet smtp.gmail.com 587
   ```
3. Для Gmail: создайте App Password
4. Проверьте логи PHP на ошибки подключения

### Проблема: Magic link не работает

**Симптомы:**
- Ссылка ведет на 404
- Ошибка "Token expired"
- Ошибка "Token already used"

**Решение:**
1. Проверьте что ссылка не истекла (срок действия 24 часа)
2. Проверьте что ссылка не была использована ранее
3. Проверьте таблицу `magic_links` в MySQL:
   ```sql
   SELECT * FROM magic_links ORDER BY created_at DESC LIMIT 10;
   ```
4. Проверьте что файл `premium/api/auth/magic-consume.php` существует

### Проблема: Доступ к премиум не работает

**Симптомы:**
- После входа редирект на главную
- Премиум страницы показывают 403

**Решение:**
1. Проверьте что сессии работают на сервере
2. Проверьте таблицу `access` в MySQL:
   ```sql
   SELECT * FROM access;
   ```
3. Проверьте логи PHP в `check-auth.php`
4. Очистите cookies браузера и попробуйте снова

### Проблема: 403 Forbidden на premium папке

**Симптомы:**
- При открытии `https://yourdomain.com/premium/` → 403

**Решение:**
1. Проверьте права доступа:
   ```bash
   chmod 755 premium/
   chmod 644 premium/index.html
   ```
2. Проверьте .htaccess:
   - Убедитесь что RewriteEngine On
   - Проверьте что mod_rewrite включен
3. Проверьте index.html существует

### Проблема: Статические ресурсы не загружаются

**Симптомы:**
- CSS/JS не работают
- Изображения не отображаются
- 404 на `/assets/...`

**Решение:**
1. Проверьте что папка `assets/` загружена полностью
2. Проверьте пути в HTML (должны быть `/assets/...`)
3. Проверьте права доступа:
   ```bash
   chmod 755 assets/
   chmod 644 assets/*
   ```
4. Очистите кеш браузера (Ctrl+Shift+Del)

### Проблема: "Mixed Content" в браузере

**Симптомы:**
- Предупреждения в консоли браузера
- Часть ресурсов не загружается

**Решение:**
1. Убедитесь что ВСЕ ресурсы загружаются через HTTPS
2. Проверьте что в HTML нет `http://` ссылок
3. Добавьте в .htaccess форсирование HTTPS:
   ```apache
   RewriteEngine On
   RewriteCond %{HTTPS} off
   RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [R=301,L]
   ```

---

## 🎯 ДОПОЛНИТЕЛЬНЫЕ РЕКОМЕНДАЦИИ

### Мониторинг и логи

1. **Настройте мониторинг health.php:**
   - Используйте UptimeRobot или подобный сервис
   - Проверяйте каждые 5 минут
   - Получайте уведомления при сбоях

2. **Включите детальное логирование PHP:**
   ```php
   // В premium/config.php добавьте:
   error_reporting(E_ALL);
   ini_set('display_errors', 0);
   ini_set('log_errors', 1);
   ini_set('error_log', '/path/to/logs/php-errors.log');
   ```

3. **Мониторьте таблицу orders:**
   - Регулярно проверяйте новые заказы
   - Следите за статусами (pending/paid)

### Резервное копирование

1. **Настройте автобэкапы MySQL:**
   ```bash
   # Добавьте в cron:
   0 2 * * * mysqldump -u toosmart_user -p'ПАРОЛЬ' toosmart > /backups/toosmart_$(date +\%Y\%m\%d).sql
   ```

2. **Копируйте конфигурационные файлы:**
   - `premium/storage/settings.json`
   - `premium/storage/products.json`

3. **Храните копии локально:**
   - Скачивайте бэкапы на ваш компьютер еженедельно

### Оптимизация производительности

1. **Включите кеширование:**
   ```apache
   # В .htaccess корня добавьте:
   <IfModule mod_expires.c>
     ExpiresActive On
     ExpiresByType text/css "access plus 1 year"
     ExpiresByType application/javascript "access plus 1 year"
     ExpiresByType image/svg+xml "access plus 1 year"
     ExpiresByType image/png "access plus 1 year"
   </IfModule>
   ```

2. **Включите gzip сжатие:**
   ```apache
   <IfModule mod_deflate.c>
     AddOutputFilterByType DEFLATE text/html text/css application/javascript
   </IfModule>
   ```

### Безопасность

1. **Регулярно обновляйте:**
   - PHP до последней версии
   - MySQL/MariaDB
   - Пароли admin_token

2. **Ограничьте доступ к ResultURL:**
   - Добавьте IP whitelist для Robokassa
   - Блокируйте все остальные IP

3. **Настройте fail2ban:**
   - Блокируйте bruteforce атаки на login.php

---

## 📞 ПОДДЕРЖКА

### Если ничего не помогло:

1. **Проверьте документацию:**
   - `ROBOKASSA-INTEGRATION.md` - детали Robokassa
   - `README.md` - общая информация
   - `03_BUILD_AND_DEPLOY_QUICKSTART.md` - краткая инструкция

2. **Проверьте логи:**
   - PHP error log
   - MySQL error log
   - Apache/Nginx access log
   - Apache/Nginx error log

3. **Обратитесь в поддержку Robokassa:**
   - Для вопросов по Result URL
   - Для проблем с фискализацией
   - Для вопросов по интеграции

4. **Свяжитесь с хостингом:**
   - Для проблем с PHP/MySQL
   - Для настройки mod_rewrite
   - Для открытия портов

---

## ✅ ГОТОВО!

После выполнения всех шагов ваш сайт готов к работе:

- ✅ Frontend развернут и доступен
- ✅ Backend настроен и работает
- ✅ MySQL база инициализирована
- ✅ Robokassa интегрирована
- ✅ Email отправляются
- ✅ Magic links работают
- ✅ Премиум доступ функционирует
- ✅ Безопасность настроена

**Следующие шаги:**
1. Проведите тестовую оплату
2. Убедитесь что весь флоу работает
3. Переведите Robokassa в боевой режим (`is_test: false`)
4. Начните продажи!

---

**Удачи с запуском проекта! 🚀**
