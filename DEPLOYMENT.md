# 🚀 Полное руководство по развертыванию TooSmart

**Обновлено:** 01.12.2025  
**Версия:** 2.0

---

## 📋 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [Выбор хостинга](#выбор-хостинга)
3. [Подготовка к деплою](#подготовка-к-деплою)
4. [Настройка хостинга](#настройка-хостинга)
5. [Развертывание файлов](#развертывание-файлов)
6. [Настройка базы данных](#настройка-базы-данных)
7. [Настройка конфигурации](#настройка-конфигурации)
8. [Интеграция Robokassa](#интеграция-robokassa)
9. [Настройка email](#настройка-email)
10. [Тестирование](#тестирование)
11. [Финальный чеклист](#финальный-чеклист)
12. [Решение проблем](#решение-проблем)

---

## 🎯 Обзор проекта

TooSmart — это образовательная платформа с системой монетизации через Robokassa.

### Технологический стек

**Frontend:**
- Статические HTML/CSS/JS файлы
- Vite для сборки ассетов
- Адаптивный дизайн

**Backend:**
- PHP 7.4+ (рекомендуется 8.0+)
- MySQL 5.7+ / MariaDB 10.2+
- Система аутентификации через Magic Links
- Интеграция с Robokassa (оплата + фискализация)

### Структура проекта

```
toosmart/
├── Бесплатный контент (в корне)
│   ├── index.html               # Главная страница
│   ├── course/                  # Бесплатные статьи (блюренные)
│   ├── legal/                   # Юридические документы
│   └── recommendations/         # Рекомендательные статьи
└── premium/                     # Премиум версия (защищенная)
    ├── course/                  # Полные статьи курса
    ├── api/                     # API endpoints
    ├── robokassa/               # Обработка платежей
    └── storage/                 # Конфигурация (создается на сервере)
```

---

## 🏢 Выбор хостинга

### Рекомендуемые хостинги для России

#### 1. **Timeweb** (Рекомендация #1)

✅ **Почему именно Timeweb:**
- Серверы в России (быстрая отдача контента)
- Отличная поддержка PHP 8.x
- Удобная панель управления
- Бесплатный SSL сертификат (Let's Encrypt)
- phpMyAdmin из коробки
- Цена: от 199₽/мес

**Тариф:** "Начальный" или "Оптимальный"  
**Сайт:** [timeweb.com](https://timeweb.com)

**Что входит:**
- PHP 8.1+
- MySQL 8.0
- 10-25 ГБ SSD
- Неограниченный трафик
- SSH доступ (опционально)

---

#### 2. **Beget** (Альтернатива)

✅ **Преимущества:**
- Очень простая панель управления
- Отличная документация на русском
- Автоматические бэкапы
- Цена: от 190₽/мес

**Тариф:** "Базовый"  
**Сайт:** [beget.com](https://beget.com)

---

#### 3. **Reg.ru** (Для начинающих)

✅ **Преимущества:**
- Известный бренд
- Техподдержка 24/7
- Простая регистрация
- Цена: от 250₽/мес

**Тариф:** "Старт"  
**Сайт:** [reg.ru](https://reg.ru)

---

### Минимальные требования к хостингу

**Обязательно:**
- ✅ PHP 7.4+ (лучше 8.0+)
- ✅ MySQL 5.7+ или MariaDB 10.2+
- ✅ HTTPS поддержка (обязательно для Robokassa!)
- ✅ Возможность настройки `.htaccess`
- ✅ Доступ к phpMyAdmin
- ✅ Минимум 5 ГБ дискового пространства

**Желательно:**
- SSH доступ
- Cron jobs
- Логи PHP errors
- FTP/SFTP доступ

**Проверка PHP модулей:**

Создайте файл `info.php` на хостинге:
```php
<?php phpinfo(); ?>
```

Откройте `https://yourdomain.com/info.php` и проверьте наличие:
- `pdo_mysql` — работа с MySQL
- `mbstring` — поддержка кириллицы
- `openssl` — генерация токенов
- `json` — обработка JSON

⚠️ **После проверки удалите файл `info.php`!**

---

## 🛠 Подготовка к деплою

### Шаг 1: Локальная сборка проекта

```bash
# 1. Установите зависимости (если еще не установлены)
npm install

# 2. Очистите старую сборку
npm run clean

# 3. Выполните сборку для production
npm run build
```

После выполнения появится папка `dist/` с готовыми файлами.

### Шаг 2: Проверьте структуру dist/

```
dist/
├── index.html                    # Главная страница
├── robots.txt
├── sitemap.xml
├── course/                       # Бесплатные статьи (блюренные)
├── legal/                        # Юридические документы
├── recommendations/              # Рекомендации
├── assets/                       # CSS/JS/изображения
├── shared/                       # JSON данные
└── premium/                      # Премиум версия (целиком!)
    ├── .htaccess
    ├── course/                   # Полные статьи
    ├── api/
    ├── robokassa/
    ├── src/
    ├── sql/
    └── storage/                  # Создавать вручную на сервере!
```

---

## ⚙️ Настройка хостинга

### Шаг 1: Зарегистрируйтесь на хостинге

**Для Timeweb:**

1. Перейдите на [timeweb.com](https://timeweb.com)
2. Выберите тариф "Оптимальный"
3. Зарегистрируйте аккаунт
4. Оплатите услугу

### Шаг 2: Привяжите домен

**Если у вас уже есть домен:**

1. В панели хостинга найдите раздел "Домены"
2. Нажмите "Привязать домен"
3. Укажите ваш домен (например, `toosmart.ru`)
4. Измените NS-серверы у регистратора домена:
   ```
   ns1.timeweb.ru
   ns2.timeweb.ru
   ```
5. Дождитесь делегирования (до 24 часов)

**Если домена нет:**

1. Купите домен на том же хостинге или на [reg.ru](https://reg.ru)
2. Привяжите его к хостингу

### Шаг 3: Настройте SSL

**Для Timeweb (автоматически):**

1. Перейдите в раздел "SSL сертификаты"
2. Выберите "Let's Encrypt"
3. Нажмите "Установить"
4. Подождите 5-10 минут

SSL будет автоматически продлеваться каждые 90 дней.

⚠️ **ВАЖНО:** Без HTTPS платежи через Robokassa работать не будут!

### Шаг 4: Создайте базу данных MySQL

**В панели управления хостингом:**

1. Найдите раздел "Базы данных" → "MySQL"
2. Нажмите "Создать базу данных"
3. Укажите:
   - **Имя БД:** `toosmart` (или любое другое)
   - **Пользователь:** создайте нового (например, `toosmart_user`)
   - **Пароль:** сгенерируйте сложный пароль
   - **Кодировка:** `utf8mb4_unicode_ci`
4. Сохраните данные доступа (понадобятся позже!)

**Запишите:**
```
Имя БД: toosmart
Пользователь: toosmart_user
Пароль: [ваш_пароль]
Хост: localhost
```

---

## 📤 Развертывание файлов

### Вариант 1: Через FTP/SFTP (FileZilla)

#### Установка FileZilla

1. Скачайте [FileZilla Client](https://filezilla-project.org/)
2. Установите программу

#### Подключение к хостингу

1. Откройте FileZilla
2. В верхней панели введите:
   - **Хост:** `ftp.yourdomain.com` (или IP из панели хостинга)
   - **Имя пользователя:** ваш FTP логин
   - **Пароль:** ваш FTP пароль
   - **Порт:** `21` (FTP) или `22` (SFTP)
3. Нажмите "Быстрое соединение"

#### Загрузка файлов

1. В левой панели (локальный) откройте папку `dist/`
2. В правой панели (сервер) откройте `public_html/` или `/httpdocs/`
3. Выделите **ВСЕ** файлы и папки внутри `dist/`
4. Перетащите их в правую панель (на сервер)
5. Дождитесь завершения загрузки (5-15 минут)

**ВАЖНО:** Загружайте **содержимое** папки `dist/`, а не саму папку!

✅ **Правильно:**
```
public_html/
├── index.html
├── course/
├── premium/
└── assets/
```

❌ **Неправильно:**
```
public_html/
└── dist/
    ├── index.html
    └── ...
```

---

### Вариант 2: Через панель хостинга (веб-интерфейс)

1. Создайте ZIP архив из **содержимого** папки `dist/`:
   ```bash
   cd dist
   zip -r ../deploy.zip *
   ```
2. В панели хостинга найдите "Файловый менеджер"
3. Перейдите в `public_html/`
4. Загрузите `deploy.zip`
5. Щелкните правой кнопкой → "Извлечь"
6. Удалите `deploy.zip`

---

### Вариант 3: Через SSH (для продвинутых)

```bash
# С вашего компьютера
rsync -avz --delete dist/ user@yourhost.com:/var/www/html/
```

---

### Проверка загрузки

Откройте в браузере: `https://yourdomain.com/`

Вы должны увидеть главную страницу курса.

---

## 🗄️ Настройка базы данных

### Шаг 1: Откройте phpMyAdmin

1. В панели хостинга найдите "phpMyAdmin"
2. Войдите используя данные БД
3. Выберите базу `toosmart` в левой панели

### Шаг 2: Импортируйте схему

1. Перейдите на вкладку **"Импорт"** (Import)
2. Нажмите **"Выберите файл"** (Choose file)
3. Выберите файл `premium/sql/schema.sql` **с сервера** (используйте FTP чтобы скачать или найдите в файловом менеджере)
4. Кодировка: `utf8mb4`
5. Нажмите **"Вперед"** (Go)

**Альтернатива (через SSH):**
```bash
mysql -u toosmart_user -p toosmart < /var/www/html/premium/sql/schema.sql
```

### Шаг 3: Проверьте таблицы

В phpMyAdmin выполните:
```sql
USE toosmart;
SHOW TABLES;
```

Должны быть созданы 4 таблицы:
- ✅ `users` — пользователи
- ✅ `orders` — заказы
- ✅ `access` — доступы к премиум
- ✅ `magic_links` — токены для входа

---

## 📝 Настройка конфигурации

### Шаг 1: Создайте папку storage

**Через файловый менеджер хостинга:**

1. Откройте `public_html/premium/`
2. Создайте папку `storage`
3. Установите права **700** (только владелец)

**Через FTP:**
1. Подключитесь к серверу
2. Перейдите в `/public_html/premium/`
3. Создайте папку `storage`

**Через SSH:**
```bash
mkdir /var/www/html/premium/storage
chmod 700 /var/www/html/premium/storage
```

---

### Шаг 2: Создайте settings.json

В папке `premium/storage/` создайте файл `settings.json`:

```json
{
  "db": {
    "dsn": "mysql:host=localhost;dbname=toosmart;charset=utf8mb4",
    "user": "toosmart_user",
    "pass": "ВАШ_ПАРОЛЬ_ОТ_БД"
  },
  "robokassa": {
    "merchant_login": "ВАШ_ЛОГИН_ROBOKASSA",
    "pass1": "ПАРОЛЬ_1_ROBOKASSA",
    "pass2": "ПАРОЛЬ_2_ROBOKASSA",
    "is_test": true,
    "signature_alg": "md5",
    "success_url": "https://yourdomain.com/premium/success.php",
    "fail_url": "https://yourdomain.com/premium/fail.php",
    "default_sno": "usn_income",
    "default_tax": "none",
    "product_code": "premium_course"
  },
  "security": {
    "session_name": "toosmart_premium",
    "admin_token": "СГЕНЕРИРУЙТЕ_СЛУЧАЙНУЮ_СТРОКУ_32_СИМВОЛА"
  },
  "emails": {
    "from": "noreply@yourdomain.com",
    "transport": "phpmail"
  },
  "site": {
    "base_url": "https://yourdomain.com"
  }
}
```

**Замените:**
- `yourdomain.com` → ваш реальный домен
- `ВАШ_ПАРОЛЬ_ОТ_БД` → пароль из Шага 4 настройки хостинга
- `ВАШ_ЛОГИН_ROBOKASSA` → заполните после регистрации в Robokassa
- `ПАРОЛЬ_1_ROBOKASSA` и `ПАРОЛЬ_2_ROBOKASSA` → из личного кабинета Robokassa

**Генерация admin_token:**
```bash
openssl rand -hex 32
```

Или онлайн: https://www.random.org/strings/ (32 символа, A-Z, a-z, 0-9)

**Установите права:**
```bash
chmod 600 /var/www/html/premium/storage/settings.json
```

---

### Шаг 3: Создайте products.json

В папке `premium/storage/` создайте файл `products.json`:

```json
{
  "premium_course": {
    "name": "Курс \"Clean - Теория правильной уборки\"",
    "price": 990.0,
    "tax": "none",
    "payment_method": "full_payment",
    "payment_object": "service"
  }
}
```

**Можете изменить:**
- `price` — стоимость курса в рублях
- `name` — название продукта

**Установите права:**
```bash
chmod 600 /var/www/html/premium/storage/products.json
```

---

### Шаг 4: Обновите .htaccess в premium/

Откройте файл `premium/.htaccess` и найдите строку:

```apache
RewriteCond %{HTTP_REFERER} !^https://(www\.)?toosmart\.com [NC]
```

Замените `toosmart.com` на **ваш домен**:

```apache
RewriteCond %{HTTP_REFERER} !^https://(www\.)?yourdomain\.com [NC]
```

Сохраните файл.

---

## 💳 Интеграция Robokassa

### Шаг 1: Регистрация в Robokassa

1. Перейдите на [auth.robokassa.ru](https://auth.robokassa.ru/)
2. Нажмите **"Регистрация"**
3. Заполните форму:
   - Email
   - Пароль
   - Название компании
4. Подтвердите email

### Шаг 2: Создание магазина

1. Войдите в личный кабинет
2. Перейдите в **"Магазины"** → **"Добавить магазин"**
3. Заполните данные:
   - **Название магазина:** "TooSmart Образовательный курс"
   - **Описание:** "Продажа доступа к онлайн-курсу"
   - **URL сайта:** `https://yourdomain.com`
   - **Email:** ваш email

### Шаг 3: Технические настройки магазина

В разделе **"Технические настройки":**

1. **Алгоритм подписи:**
   - Выберите `MD5` ✅

2. **URL адреса:**
   - **Result URL:** `https://yourdomain.com/premium/robokassa/result.php`
   - **Success URL:** `https://yourdomain.com/premium/success.php`
   - **Fail URL:** `https://yourdomain.com/premium/fail.php`
   - **Метод отправки:** `POST` для всех

3. **Фискализация (54-ФЗ):**
   - ✅ Включите **"Отправлять данные для чеков в ОФД"**
   - Заполните данные вашей компании:
     - ИНН
     - Адрес
     - Система налогообложения: `УСН доход` (или ваша)

4. **Тестовый режим:**
   - ✅ Включите **"Тестовый режим"** для начала

### Шаг 4: Получите пароли

1. В ЛК Robokassa перейдите в **"Технические настройки"**
2. Скопируйте:
   - **Идентификатор магазина (Merchant Login)**
   - **Пароль #1** (для генерации ссылки оплаты)
   - **Пароль #2** (для проверки Result URL)

⚠️ **ВАЖНО:** В тестовом режиме используйте **тестовые** пароли!

### Шаг 5: Вставьте данные в settings.json

Откройте `premium/storage/settings.json` и обновите:

```json
"robokassa": {
  "merchant_login": "СКОПИРУЙТЕ_СЮДА_MERCHANT_LOGIN",
  "pass1": "СКОПИРУЙТЕ_СЮДА_ПАРОЛЬ_1",
  "pass2": "СКОПИРУЙТЕ_СЮДА_ПАРОЛЬ_2",
  "is_test": true,
  ...
}
```

Сохраните файл.

### Шаг 6: Проверьте доступность Result URL

Robokassa должна иметь возможность вызвать ваш Result URL!

**Тест через браузер:**
```
https://yourdomain.com/premium/robokassa/result.php
```

Должна открыться страница (даже если будет ошибка — это нормально).

**Тест через curl (если есть SSH):**
```bash
curl -X POST https://yourdomain.com/premium/robokassa/result.php
```

Если возвращается 403/404/500 → проверьте:
- Файл существует
- Права доступа (644)
- .htaccess не блокирует POST запросы

### Шаг 7: Добавьте IP Robokassa в whitelist

В настройках firewall хостинга (если есть) добавьте IP Robokassa:
```
91.227.152.141
91.227.152.142
```

---

## ✉️ Настройка email

Проект отправляет email для:
- Magic links (ссылки для входа после оплаты)
- Уведомления о покупках

### Вариант 1: PHP Mail (простой)

В `settings.json` уже указано:
```json
"emails": {
  "from": "noreply@yourdomain.com",
  "transport": "phpmail"
}
```

**Требования:**
- Хостинг должен поддерживать `mail()` функцию PHP
- Обычно работает на Timeweb, Beget, Reg.ru

**Проверка (SSH):**
```bash
echo "Test email" | mail -s "Test Subject" your@email.com
```

⚠️ **Проблема:** Письма могут попадать в спам!

---

### Вариант 2: SMTP (рекомендуется!)

Используйте внешний SMTP сервер (Gmail, Yandex, Mail.ru).

#### Для Gmail:

1. Включите 2FA в аккаунте Google
2. Создайте "App Password":
   - Перейдите на https://myaccount.google.com/apppasswords
   - Выберите "Почта" и "Другое устройство"
   - Скопируйте сгенерированный пароль

3. Обновите `settings.json`:
```json
"emails": {
  "from": "noreply@yourdomain.com",
  "transport": "smtp",
  "smtp": {
    "host": "smtp.gmail.com",
    "port": 587,
    "user": "your@gmail.com",
    "pass": "APP_PASSWORD_HERE",
    "secure": "tls"
  }
}
```

#### Для Yandex:

```json
"smtp": {
  "host": "smtp.yandex.ru",
  "port": 465,
  "user": "your@yandex.ru",
  "pass": "YOUR_PASSWORD",
  "secure": "ssl"
}
```

#### Для Mail.ru:

```json
"smtp": {
  "host": "smtp.mail.ru",
  "port": 465,
  "user": "your@mail.ru",
  "pass": "YOUR_PASSWORD",
  "secure": "ssl"
}
```

---

## ✅ Тестирование

### 1. Health Check

Откройте в браузере:
```
https://yourdomain.com/premium/health.php
```

**Ожидается:**
```json
{
  "status": "ok",
  "database": "connected",
  "users_count": 0,
  "timestamp": "2025-12-01 15:30:00"
}
```

❌ **Если ошибка:**
- Проверьте `settings.json` (правильность данных БД)
- Проверьте что MySQL работает
- Посмотрите логи PHP

---

### 2. Проверка главной страницы

```
https://yourdomain.com/
```

Должна открыться главная страница курса.

---

### 3. Проверка бесплатной статьи

```
https://yourdomain.com/course/раздел-1-основы.html
```

Должна открыться статья с заблюренным контентом и кнопкой "Войти в полную версию".

---

### 4. Тестовая оплата

1. На главной странице нажмите кнопку **"Получить полный доступ"**
2. В модальном окне введите **ваш реальный email**
3. Согласитесь с офертой
4. Нажмите **"Оплатить 990 ₽"**
5. Произойдет редирект на страницу Robokassa

**Тестовая оплата (в тестовом режиме):**
- Карта: `5555 5555 5555 5599`
- Срок: любой будущий (например, 12/25)
- CVV: любой (например, 123)
- Имя: любое

6. Нажмите **"Оплатить"**
7. Robokassa обработает платеж и вызовет Result URL
8. На ваш email придет письмо с **Magic Link**

---

### 5. Проверка Magic Link

1. Откройте письмо
2. Найдите ссылку вида:
   ```
   https://yourdomain.com/premium/api/auth/magic-consume.php?token=...&email=...
   ```
3. Перейдите по ссылке
4. Должен произойти автоматический вход
5. Редирект на `/premium/`
6. Вы должны увидеть **полную версию курса**

---

### 6. Проверка премиум контента

Перейдите на:
```
https://yourdomain.com/premium/course/раздел-1-основы.html
```

Должна открыться полная статья **без блюра** и ограничений.

---

### 7. Проверка выхода

Нажмите кнопку **"Выйти"** в меню.

Должен произойти выход и редирект на главную страницу.

---

## 📋 Финальный чеклист

Перед переходом в production проверьте:

### Хостинг
- [ ] Домен привязан и работает
- [ ] SSL сертификат установлен
- [ ] HTTPS работает на всех страницах
- [ ] Все файлы из `dist/` загружены

### База данных
- [ ] MySQL база создана
- [ ] Пользователь БД создан
- [ ] Схема импортирована (4 таблицы)
- [ ] Health check возвращает `"status": "ok"`

### Конфигурация
- [ ] `premium/storage/settings.json` создан
- [ ] Данные MySQL корректны
- [ ] Robokassa credentials вставлены
- [ ] admin_token сгенерирован (32+ символов)
- [ ] Домен указан правильно (`site.base_url`)
- [ ] Права на файлы: 600 для settings.json и products.json

### Robokassa
- [ ] Магазин создан в ЛК Robokassa
- [ ] Фискализация включена (54-ФЗ)
- [ ] Result URL указан правильно
- [ ] Success/Fail URL указаны
- [ ] Result URL доступен (не 403/404)
- [ ] Тестовый режим включен для тестирования

### Email
- [ ] Email настроен (phpmail или SMTP)
- [ ] Тестовое письмо отправлено успешно

### Тестирование
- [ ] Главная страница открывается
- [ ] Бесплатные статьи показываются
- [ ] Модальное окно оплаты работает
- [ ] Тестовая оплата прошла успешно
- [ ] Magic link приходит на почту
- [ ] Вход через Magic Link работает
- [ ] Премиум контент доступен после входа
- [ ] Выход работает

### Переход в production
- [ ] В ЛК Robokassa отключите тестовый режим
- [ ] В `settings.json` установите `"is_test": false`
- [ ] Замените **тестовые** пароли на **боевые**
- [ ] Проведите реальную тестовую оплату (1 рубль)

---

## 🔧 Решение проблем

### ❌ Health check возвращает ошибку

**Проблема:** HTTP 500 или "Database connection failed"

**Решение:**
1. Проверьте `settings.json`:
   - DSN правильный: `mysql:host=localhost;dbname=toosmart`
   - Имя пользователя и пароль верные
2. Проверьте права на `settings.json` (должно быть 600)
3. Проверьте что MySQL работает в phpMyAdmin
4. Посмотрите PHP error log в панели хостинга

---

### ❌ Robokassa не вызывает Result URL

**Проблема:** Оплата проходит, но письмо не приходит

**Решение:**
1. Проверьте что URL доступен:
   ```bash
   curl -X POST https://yourdomain.com/premium/robokassa/result.php
   ```
2. В ЛК Robokassa проверьте раздел "Журнал операций" → посмотрите ошибки
3. Проверьте что URL указан правильно (без лишних пробелов)
4. Убедитесь что .htaccess не блокирует POST запросы
5. Добавьте IP Robokassa в whitelist firewall

---

### ❌ Invalid signature от Robokassa

**Проблема:** "Invalid signature" в логах

**Решение:**
1. Убедитесь что `pass1` и `pass2` **совпадают** с ЛК Robokassa
2. Проверьте режим:
   - Тестовый режим (`is_test: true`) = тестовые пароли
   - Боевой режим (`is_test: false`) = боевые пароли
3. Проверьте `signature_alg` (должен быть `md5`)
4. Проверьте `merchant_login`

---

### ❌ Email не отправляются

**Проблема:** Magic link не приходит

**Решение (phpmail):**
1. Проверьте что хостинг поддерживает `mail()`
2. Переключитесь на SMTP (рекомендуется)

**Решение (SMTP):**
1. Проверьте данные SMTP в `settings.json`
2. Для Gmail: используйте App Password (не обычный пароль!)
3. Проверьте что порт открыт (587 для TLS, 465 для SSL)
4. Посмотрите PHP error log

---

### ❌ Magic link не работает

**Проблема:** Ссылка ведет на 404 или "Token expired"

**Решение:**
1. Проверьте что ссылка не истекла (срок действия 24 часа)
2. Проверьте что файл `premium/api/auth/magic-consume.php` существует
3. В phpMyAdmin проверьте таблицу `magic_links`:
   ```sql
   SELECT * FROM magic_links ORDER BY created_at DESC LIMIT 5;
   ```
4. Попробуйте запросить новую ссылку

---

### ❌ 403 Forbidden на premium/

**Проблема:** При открытии `/premium/` → 403

**Решение:**
1. Проверьте права:
   ```bash
   chmod 755 premium/
   chmod 644 premium/index.html
   ```
2. Проверьте что файл `premium/index.html` существует
3. Проверьте .htaccess в `premium/`

---

### ❌ Статические файлы не загружаются

**Проблема:** CSS/JS не работают, 404 на `/assets/`

**Решение:**
1. Проверьте что папка `assets/` загружена полностью
2. Проверьте права:
   ```bash
   chmod 755 assets/
   chmod 644 assets/*
   ```
3. Очистите кеш браузера (Ctrl+Shift+Del)
4. Проверьте в DevTools → Network → какие файлы не загружаются

---

## 🎯 Переход в production

Когда все тесты пройдены:

### 1. Отключите тестовый режим Robokassa

В ЛК Robokassa:
1. Перейдите в "Технические настройки"
2. Снимите галочку "Тестовый режим"
3. Сохраните

### 2. Обновите settings.json

```json
"robokassa": {
  ...
  "is_test": false,
  "pass1": "БОЕВОЙ_ПАРОЛЬ_1",
  "pass2": "БОЕВОЙ_ПАРОЛЬ_2",
  ...
}
```

⚠️ **ВАЖНО:** Используйте **боевые** пароли (не тестовые)!

### 3. Проведите реальную тестовую оплату

1. Оплатите курс реальной картой (990₽)
2. Проверьте что весь флоу работает:
   - Оплата проходит
   - Result URL вызывается
   - Письмо приходит
   - Magic Link работает
   - Доступ открывается

### 4. Проверьте чек в ОФД

1. В ЛК Robokassa проверьте что чек отправлен в ОФД
2. Проверьте что чек пришел на email покупателя

---

## ✅ Готово!

Ваш сайт готов к работе! 🎉

**Что дальше:**
- Регулярно проверяйте health.php
- Мониторьте заказы в phpMyAdmin
- Делайте бэкапы БД (еженедельно)
- Обновляйте PHP и MySQL
- Следите за логами ошибок

**Полезные ссылки:**
- Панель Robokassa: https://auth.robokassa.ru/
- Техподдержка хостинга: смотрите в ЛК хостинга
- phpMyAdmin: `https://yourdomain.com/phpmyadmin` (или через панель)

---

**Удачи с запуском! 🚀**
