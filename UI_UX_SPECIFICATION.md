# 📐 СПЕЦИФИКАЦИЯ UI/UX - ОБРАЗОВАТЕЛЬНАЯ ПЛАТФОРМА

**Дата:** 2025-11-09
**Версия:** 2.0 (актуальная)
**Статус:** Утверждено

---

## 🎯 ОБЩАЯ КОНЦЕПЦИЯ

Адаптивная образовательная платформа для чтения статей с тремя режимами отображения:
- **Desktop** (≥1440px) - полный функционал
- **Tablet-wide** (1024-1439px) - без навигации точками
- **Handheld** (<1024px) - вертикальная раскладка

**Генерация контента:**
- Один markdown файл → одна HTML страница
- Статичная генерация (build один раз)
- Картинки PNG inline как иллюстрации

---

## 📄 СТРУКТУРА САЙТА

### Страницы:
```
/articles/
  intro.html       ← из intro.md
  basics.html      ← из basics.md
  advanced.html    ← из advanced.md
```

### Внутри каждой статьи:
- H1 заголовок статьи
- Несколько разделов с H2 заголовками
- Текст, изображения, таблицы, код, цитаты
- Кнопка "Далее" в конце

---

## 1️⃣ ТЕКСТОВЫЙ БЛОК (центральная колонка)

### Назначение:
Основной контент статьи

### Структура:
- **Один файл = одна страница**
- Внутри: несколько разделов (H2)
- Сплошной поток контента (не разбивается на блоки)

### Поведение:
- ✅ Обычный вертикальный скролл
- ✅ Изображения PNG inline (не галереи, не zoom)
- ✅ Таблицы, код, цитаты внутри потока

### Ширина:
- Desktop: 680px
- Tablet-wide: 540px
- Handheld: calc(100vw - отступы)

### Markdown элементы:
- `<img>` - центрированные, border-radius, max-width: 100%
- `<pre><code>` - блоки кода с подсветкой фона
- `<code>` - inline код
- `<blockquote>` - цитаты с левым бордером
- `<table>` - таблицы с границами
- `<ul>`, `<ol>` - списки
- `<a>` - ссылки
- `<hr>` - горизонтальные линии
- H3, H4 - подзаголовки

---

## 2️⃣ НАВИГАЦИЯ "ТОЧКАМИ" (dots-rail)

### Назначение:
Быстрая навигация **внутри** текущей статьи

### Расположение:
Слева от текста, вертикальный ряд точек

### Генерация:
- **Автоматически** из H2 заголовков
- 5 разделов H2 → 5 точек
- Обновляется при переходе на другую статью

### Поведение:
- ✅ Клик по точке → скролл к разделу **внутри страницы**
- ✅ Активная точка подсвечивается (текущий раздел)
- ✅ IntersectionObserver отслеживает видимость разделов

### Режимы:
- Desktop: **Видны**
- Tablet-wide: Скрыты (мало места)
- Handheld: Скрыты

### CSS:
- `position: fixed`
- `left: calc(50% - var(--text-box-width) / 2 - 48px)`
- `z-index: 150`

---

## 3️⃣ КНОПКА "ДАЛЕЕ"

### Назначение:
Переход на **следующую страницу** сайта (следующую статью)

### Визуальное поведение:

**Во время скролла:**
- ✅ `position: sticky`
- ✅ Прилипает к низу экрана (`bottom: 24px`)
- ✅ Видна всегда при скролле

**Когда скролл закончен:**
- ✅ Встает в обычную позицию в конце текста
- ✅ `position: static` срабатывает когда достигнут конец

### Логика клика:

**НЕ скроллит внутри статьи!**

```javascript
function handleNext() {
  const nextPageUrl = btnNext?.dataset.nextPage;
  if (nextPageUrl) {
    window.location.href = nextPageUrl; // переход на другую страницу
  }
}
```

### HTML:
```html
<button class="btn-next" type="button" data-next-page="basics.html">
  Далее
</button>
```

### Режимы:
- Desktop: sticky, центрирована
- Tablet-wide: sticky, центрирована
- Handheld: обычная кнопка в конце, ширина min(320px, 100%)

---

## 4️⃣ ОГЛАВЛЕНИЕ (левое меню, menu-rail)

### Назначение:
Навигация по всем статьям сайта

### Заголовок:
**"Структура курса"**

### Структура меню:
```
Структура курса
├─ 1. Введение в курс
│  ├─ Что вы узнаете         ← H2
│  ├─ Как это работает       ← H2
│  └─ Приступим              ← H2
├─ 2. Основы химического состава
│  ├─ Что такое pH           ← H2
│  ├─ Типы компонентов       ← H2
│  └─ ...
└─ 3. ПАВы и их виды
   └─ ...
```

**Показываем:**
- ✅ Название статьи (жирный шрифт, на отдельной карточке)
- ✅ H2 заголовки под каждой статьей (список с отступом)

### Генерация:
Автоматически из конфига:

```json
{
  "articles": [
    {
      "id": "intro",
      "title": "Введение в курс",
      "sections": [
        {"id": "section-1", "title": "Что вы узнаете"},
        {"id": "section-2", "title": "Как это работает"}
      ]
    }
  ]
}
```

### Поведение:
- ✅ Клик по названию статьи → загрузка страницы
- ✅ Клик по H2 → загрузка страницы + скролл к разделу
- ✅ Независимый скролл внутри меню
- ✅ **Когда меню открыто → скролл текста НЕ работает**

### Открытие/закрытие:

**Desktop:**
- Hover по ярлыку → временный выезд (`is-slid`)
- Клик по ярлыку → полное открытие (`menu-open`)
- Клик по бэкдропу / Escape → закрыть

**Tablet-wide:**
- Tap по ярлыку → открыть
- Edge-swipe от левого края → открыть
- Tap по бэкдропу / Escape → закрыть

**Handheld:**
- Меню снизу (плашка-док)
- Tap по ярлыку / swipe вверх → открыть
- Swipe вниз / tap по верхней области / Escape → закрыть

### Позиционирование:
- Desktop/Tablet: слева, фиксировано
- Handheld: снизу, на всю ширину

### Размеры:
- Закрыто: 70px (только ярлык)
- Открыто: 320px

---

## 5️⃣ РЕКОМЕНДАЦИИ (правая колонка, stack)

### Назначение:
Рекламные/сопутствующие материалы

### Заголовок:
**"🔥 Рекомендуем"**

### Количество карточек:
2-5 карточек

### Пример карточек:
```html
<li class="stack-card">
  <h3>ВЫ НЕ УМЕЕТЕ ЧИТАТЬ ЭТИКЕТКИ</h3>
  <p>Гид по химическому составу для тех, кто хочет жить</p>
</li>
```

### КЛЮЧЕВОЕ ПОВЕДЕНИЕ - PARALLAX:

**Блок ВСЕГДА на экране!**

**Если высота контента ≤ высота экрана:**
- Блок просто висит, parallax не нужен

**Если высота контента > высота экрана:**
- ✅ Блок фиксирован к viewport (`position: fixed`)
- ✅ Содержимое двигается медленнее скролла (коэффициент 0.6)
- ✅ В начале статьи (scroll = 0) → видим **верх** рекомендаций
- ✅ В конце статьи (scroll = max) → видим **низ** рекомендаций
- ✅ Блок **никогда** не уходит за пределы экрана
- ✅ Просто "проезжаем" по содержимому медленно

**НЕТ:**
- ❌ Внутреннего скролла (`overflow: auto`)
- ❌ Sticky позиционирования
- ❌ Автоматической ротации карточек

### CSS:
```css
.stack {
  position: fixed;
  top: var(--stack-top);
  right: /* вычисляется */;
  height: calc(100dvh - var(--stack-top) - var(--stack-bottom));
  overflow: hidden;
}

.stack-list {
  overflow: visible;
  will-change: transform; /* двигается через JS */
}
```

### JavaScript:
```javascript
// При скролле двигаем stack-list:
const parallaxOffset = -(scrollY / maxScroll) * maxOffset * 0.6;
stackList.style.transform = `translateY(${parallaxOffset}px)`;
```

### Режимы:
- Desktop: справа, fixed, parallax
- Tablet-wide: справа, fixed, parallax
- Handheld: **ПОД текстом**, обычный поток, parallax отключен

---

## 6️⃣ HEADER (шапка)

### Содержимое:
- Логотип (слева)
- Emoji "🤚" (декоративный элемент)
- Ссылки: "Помощь", "О курсе" (справа)

### Поведение:
- `position: sticky`
- `top: 0`
- При открытии меню на desktop/tablet → сдвигается синхронно с меню

### Размеры:
- Высота: динамическая (padding: 12px)
- Используется для вычисления `--stack-top`

---

## 📐 РЕЖИМЫ АДАПТИВНОСТИ

### Desktop (≥1440px)

**Детекция:**
- Ширина ≥1440px
- НЕ touch устройство

**Раскладка:**
- Grid: `1fr | 680px | clamp(320px, 28vw, 420px) | 1fr`
- Текст в центре
- Рекомендации справа (fixed)
- Меню слева (overlay)

**Элементы:**
- ✅ Точки навигации
- ✅ Рекомендации с parallax
- ✅ Меню с hover/click

---

### Tablet-wide (1024-1439px)

**Детекция:**
- 1024 ≤ ширина ≤ 1439px
- ИЛИ touch устройство при ширине ≥1024px

**Раскладка:**
- Grid: `1fr | 540px | clamp(280px, 30vw, 360px) | 1fr`

**Элементы:**
- ❌ Точек нет
- ✅ Рекомендации справа с parallax
- ✅ Меню слева (tap/swipe)

---

### Handheld (<1024px)

**Детекция:**
- Ширина <1024px

**Раскладка:**
- Одна колонка (flex-direction: column)
- Порядок: текст → кнопка "Далее" → рекомендации

**Элементы:**
- ❌ Точек нет
- ❌ Parallax отключен
- ✅ Рекомендации под текстом
- ✅ Меню снизу (плашка-док)

---

## 🎨 ВИЗУАЛЬНЫЕ ПРАВИЛА

### Цветовая схема:
```css
:root {
  --surface: #f6f6f6;
  --surface-alt: #ededed;
  --surface-content: #fff;
  --text-color: #222;
  --text-secondary: #666;
  --border-color: #d0d0d0;
  --code-block-bg: #2d2d2d;
  --code-block-color: #f8f8f2;
  --link-color: #0066cc;
}
```

### Скругления:
- Все карточки, блоки: `border-radius: 12px`
- Кнопки: `border-radius: 12px`
- Изображения: `border-radius: 8px`

### Границы:
- Все блоки: `border: 1px solid var(--border-color)`

### Отступы:
- `--gap-page: 32px`
- `--stack-gap: 32px` (desktop/tablet)
- Секции: `margin-bottom: 32px`
- Изображения: `margin: 24px auto`

### Шрифты:
- Семейство: 'Inter', 'Segoe UI', sans-serif
- Основной текст: 16px, line-height: 1.6
- H2: крупнее, font-weight: 600
- Code: 'Consolas', 'Monaco', monospace

---

## 🔄 ВЗАИМОДЕЙСТВИЯ

### Сценарий 1: Чтение статьи

1. Пользователь на странице `intro.html`
2. Скроллит вниз:
   - Текст двигается вверх
   - Рекомендации справа двигаются медленнее (видим разные части)
   - Точки показывают текущий раздел
   - Кнопка "Далее" прилипла к низу
3. Кликает по точке → быстрый скролл к разделу
4. Дочитал до конца → кнопка "Далее" в обычной позиции
5. Клик "Далее" → переход на `basics.html`

### Сценарий 2: Навигация через меню

1. Открывает меню (клик/swipe)
2. Скролл текста блокируется
3. Скроллит внутри меню
4. Кликает "Основы → Типы компонентов" → переход на `basics.html#section-2`
5. Меню закрывается

### Сценарий 3: Parallax рекомендаций

1. Статья длинная, рекомендации не помещаются
2. Scroll = 0px → видим верхние карточки
3. Scroll = середина → видим средние карточки
4. Scroll = конец → видим нижние карточки
5. Блок ВСЕГДА на экране

---

## ⚙️ ТЕХНИЧЕСКИЕ ДЕТАЛИ

### data-mode:
```javascript
body.dataset.mode = 'desktop' | 'tablet-wide' | 'handheld';
```

### data-input:
```javascript
body.dataset.input = 'touch' | 'pointer';
```

### Состояния меню:
- `body.menu-open` - меню открыто
- `body.is-slid` - временный выезд (hover на desktop)
- `body[data-lock="scroll"]` - скролл заблокирован

### Z-index порядок:
- Контент: 0
- dots-rail: 150
- Header: 200
- menu-rail: 1500
- Backdrop: 1400
- Dock (handheld): 1600

---

## 📝 ПРАВИЛА ГЕНЕРАЦИИ

### Из markdown в HTML:

**Входные данные:**
```markdown
# Заголовок статьи

![Картинка](image.png)

## Раздел 1

Текст с **жирным** и *курсивом*.

```javascript
code block
```

| Table | Header |
|-------|--------|
| Data  | Data   |
```

**Генерация:**
1. Парсинг markdown → HTML (marked.js)
2. Извлечение H2 заголовков → создание секций с ID
3. Обработка путей изображений (`./image.png` → `/images/image.png`)
4. Создание навигации (dots-rail)
5. Генерация меню (из конфига)
6. Установка `data-next-page` для кнопки "Далее"

### Конфигурация:
```json
{
  "articles": [
    {
      "id": "intro",
      "title": "Введение",
      "markdown": "intro.md",
      "next": "basics"
    }
  ]
}
```

---

## ✅ КЛЮЧЕВЫЕ ОТЛИЧИЯ ОТ СТАРЫХ ВЕРСИЙ

| Элемент | Было (старое ТЗ) | Стало (актуально) |
|---------|------------------|-------------------|
| Кнопка "Далее" | Скроллит внутри статьи | Переход на след. страницу |
| Рекомендации | Sticky + внутренний скролл | Fixed + parallax (всегда видны) |
| Меню | Только названия статей | Статьи + H2 подразделы |
| Генерация | Runtime (динамически) | Build-time (статично) |
| Количество рекомендаций | 6-10 | 2-5 |

---

## 🎯 ИТОГОВЫЕ ТРЕБОВАНИЯ

### Обязательные:
1. ✅ Один markdown → одна HTML страница
2. ✅ Кнопка "Далее": sticky визуально, переход между страницами логически
3. ✅ Рекомендации: position: fixed, parallax, ВСЕГДА на экране
4. ✅ Меню: показывает статьи + их H2 заголовки
5. ✅ Точки: автогенерация из H2, только desktop
6. ✅ Блокировка скролла при открытом меню
7. ✅ Три режима адаптивности

### Запрещено:
- ❌ Внутренний скролл в рекомендациях
- ❌ Sticky для рекомендаций
- ❌ Кнопка "Далее" скроллит внутри статьи
- ❌ Zoom/lightbox для изображений
- ❌ Галереи изображений

---

**Конец спецификации**
