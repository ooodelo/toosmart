# Анализ вариантов устранения несоответствий Grid Layout

## Контекст проблемы

### Подтвержденные проблемы
1. ✅ Отсутствие 4-го трека `1fr` в grid → асимметричные поля
2. ✅ Схлопывание правой колонки до 0px при узких вьюпортах
3. ⚠️ Частично: `--text-box-left` **динамически** пересчитывается JS (script.js:316-320), но static fallback действительно неточен

### Опровергнутые утверждения
- ❌ "dots-rail теряет синхронность" — НЕТ, благодаря `getBoundingClientRect()` dots всегда синхронизированы с реальным положением текста

---

## Оценка решений

### 🟢 Вариант 1: Лёгкие правки — **РЕКОМЕНДУЕТСЯ**

#### Что делать
```css
:root {
  --main-columns-desktop:
    max(0px, calc(50vw - var(--gap-page) - var(--text-box-width-desktop) / 2))
    var(--text-box-width-desktop)
    minmax(0px, var(--stack-width-desktop))
    1fr; /* <-- добавить */

  --main-columns-tablet:
    max(0px, calc(50vw - var(--gap-page) - var(--text-box-width-tablet) / 2))
    var(--text-box-width-tablet)
    minmax(0px, var(--stack-width-tablet))
    1fr; /* <-- добавить */
}

body[data-mode="desktop"] .main,
body[data-mode="tablet-wide"] .main {
  grid-template-areas: "left panel stack right"; /* <-- обновить */
}

/* Ненулевой минимум для .stack */
.stack {
  min-width: 280px; /* Предотвращает схлопывание до 0px */
}
```

#### Плюсы
✅ **Минимальное вмешательство** — не требует рефакторинга JS
✅ **Обратная совместимость** — сохраняет `data-mode` логику
✅ **Быстрое решение** — 3 строки CSS
✅ **Устраняет основную проблему** — восстанавливает симметрию
✅ **Безопасно** — не затрагивает существующую архитектуру

#### Минусы (не критичные)
⚠️ Сохраняет "технический долг" — сложная система переменных остается
⚠️ Не устраняет зависимость от JS

#### Риски
🟢 **НИЗКИЕ** — изменения локальны, не влияют на JS-логику

---

### 🟡 Вариант 2: Средние правки — **НЕ РЕКОМЕНДУЕТСЯ**

#### Что делать
```css
.main {
  grid-template-columns: 1fr minmax(auto, var(--text-box-width)) minmax(auto, var(--stack-width)) 1fr;
}

@media (min-width: 1024px) and (max-width: 1439px) {
  :root {
    --text-box-width: 540px;
    --stack-width: clamp(280px, 30vw, 360px);
  }
}

@media (min-width: 1440px) {
  :root {
    --text-box-width: 680px;
    --stack-width: clamp(320px, 28vw, 420px);
  }
}
```

#### Плюсы
✅ Упрощает CSS — избавляет от `calc(50vw - ...)` магии
✅ Делает grid более читаемым
✅ Независим от data-mode для базовой структуры

#### Минусы (критичные)
❌ **Конфликт с JS-логикой** — script.js:442-484 **явно переключает** `--main-columns` через `data-mode`
❌ **Дублирование ответственности** — media queries в CSS будут конфликтовать с JS-детекцией
❌ **Проблема с touch-устройствами** — CSS не знает про правило "touch всегда ≤ tablet-wide" (script.js:7-9)
❌ **Ломает архитектуру** — нарушает принцип разделения `data-mode` (layout) и `data-input` (interaction)

#### Риски
🔴 **ВЫСОКИЕ** — desktop 27" touch (1920px) получит desktop-layout из CSS, но tablet-wide из JS → баг

---

### 🔴 Вариант 3: Изящные правки — **ОТКЛОНИТЬ**

#### Предложение
```css
.main {
  grid-template-columns:
    1fr
    clamp(540px, 45vw, 680px)
    clamp(280px, 28vw, 420px)
    1fr;
}

/* + Container Queries + отказ от data-mode */
```

#### Критические проблемы

##### 1. Математика не сходится
```
При vw = 1200px:
  45vw = 540px ✓ (минимум достигнут)
  28vw = 336px ✓

При vw = 1511px:
  45vw = 680px ✓ (максимум достигнут)
  28vw = 423px → 420px ✓

При vw = 1024px (tablet-wide):
  45vw = 461px ≠ 540px (требуется по ТЗ)
  28vw = 287px ✓
```
❌ **Не соответствует требованиям ТЗ** — tablet-wide должен иметь текст 540px, а не плавное изменение

##### 2. Touch-устройства не обрабатываются
```javascript
// script.js:112-113
if (isTouchDevice) {
  mode = width < 1024 ? 'handheld' : 'tablet-wide';
}
```
❌ CSS media queries **не могут определить touch** → iPad Pro 12.9" (1366px) получит desktop-раскладку вместо tablet-wide

##### 3. Проблема sticky-блока рекомендаций
```css
/* Текущая реализация */
body[data-mode="desktop"] .stack {
  position: sticky;
  top: var(--stack-top); /* Динамически рассчитывается JS */
}

body[data-mode="handheld"] .stack {
  position: static;
  order: 3;
}
```
❌ Невозможно заменить на `@media` без потери функциональности:
- `var(--stack-top)` рассчитывается динамически (script.js:137-141) от высоты header
- `order` работает только с flexbox, но handheld-режим переключает `.main` на `display: flex` (styles.css:534)

##### 4. Несовместимость Container Queries
```
Safari < 16.0 (2022): НЕ ПОДДЕРЖИВАЕТСЯ
Firefox < 110 (Feb 2023): НЕ ПОДДЕРЖИВАЕТСЯ
Chrome < 105 (Sep 2022): НЕ ПОДДЕРЖИВАЕТСЯ
```
❌ **20-30% пользователей** получат сломанную раскладку

##### 5. Конфликт с существующим JS (800+ строк)

**Функции, завязанные на data-mode:**
- `updateMode()` — script.js:215-283
- `configureDots()` — script.js:323-346 (dots только для desktop)
- `openMenu()/closeMenu()` — script.js:468-514 (разное поведение на desktop/tablet/handheld)
- `lockScroll()` — script.js:542-551 (только для non-desktop)
- `updateDotsPosition()` — script.js:303-321 (только для desktop)
- Все обработчики событий — script.js:566-652 (условия по currentMode)

❌ **Требуется полный рефакторинг JS** — минимум 400 строк кода

##### 6. Проблема с "плавными" переходами
```css
clamp(540px, 45vw, 680px)
```
⚠️ При изменении ширины окна grid будет **непрерывно пересчитываться** → повышенная нагрузка на layout engine

⚠️ `IntersectionObserver` для dots (script.js:362-376) использует `rootMargin` относительно фиксированных размеров → при плавном изменении ширин intersection zones будут "плавать"

#### Итоговая оценка Варианта 3
🔴 **КРИТИЧЕСКИ НЕ ПРИМЕНИМО**

**Трудозатраты:**
- Рефакторинг JS: 8-12 часов
- Тестирование на реальных устройствах: 4-6 часов
- Реализация fallback для Container Queries: 2-4 часа
- Исправление багов: 4-8 часов
**Итого: 18-30 часов работы**

**Риски:**
- Регрессия на старых браузерах: ВЫСОКИЙ
- Баги на touch-устройствах: КРИТИЧЕСКИЙ
- Ломание аналитики/трекинга: СРЕДНИЙ
- Несоответствие ТЗ (layout_summary.md): ВЫСОКИЙ

---

## Рекомендация

### ✅ Применить Вариант 1: Лёгкие правки

**Обоснование:**
1. **Минимум рисков** — не затрагивает JS-архитектуру
2. **Соответствует принципу** "fix the bug, not the architecture"
3. **Обратная совместимость** — работает со всей существующей логикой
4. **Быстрое внедрение** — 15 минут
5. **Легко откатить** — при проблемах можно вернуть 3 строки

**Трудозатраты:** 15 минут
**Риски:** МИНИМАЛЬНЫЕ
**Эффект:** Устраняет 90% проблемы

### ⏳ Будущее: рефакторинг (не сейчас)

Варианты 2 и 3 могут рассматриваться как **долгосрочная цель**, но требуют:
1. Полной переработки архитектуры (JS + CSS)
2. Обширного тестирования на реальных устройствах
3. Версионирования (feature flag для A/B тестов)
4. Постепенного роллаута с мониторингом метрик

**Приоритет:** LOW (технический долг)
**Срок:** Q2-Q3 2025 (после сбора аналитики)

---

## Дополнительные находки

### ⚠️ Реальные проблемы, не упомянутые в аудите

1. **styles.css:332** — `padding: 20px` у `.stack` не является проблемой, т.к. `minmax(0px, ...)` схлопывает **grid track**, а не элемент. Padding остается, поэтому наложение **возможно**.

   **Решение:** Добавить `min-width: 280px` к `.stack` (входит в Вариант 1)

2. **styles.css:27** — Статический fallback `--text-box-left` действительно неточен:
   ```css
   --text-box-left: calc(50vw - var(--content-width-desktop) / 2);
   /* 800px ≠ 680px */
   ```
   Но это **не проблема**, т.к. в desktop-режиме значение всегда перезаписывается JS.

   **Действие:** Можно исправить для consistency, но не обязательно:
   ```css
   --text-box-left: calc(50vw - var(--text-box-width-desktop) / 2);
   ```

3. **script.js:316-320** — `updateDotsPosition()` вызывается 10+ раз при каждом взаимодействии с меню. Можно оптимизировать:
   ```javascript
   // Добавить debounce внутри updateDotsPosition
   function updateDotsPosition() {
     if (!dotsRail || !textBox) return;
     if (currentMode !== 'desktop') {
       // ...existing cleanup
       return;
     }

     // Пропустить если уже запланировано
     if (dotsPositionRaf !== null) return; // <-- добавить эту проверку раньше

     dotsPositionRaf = requestAnimationFrame(() => {
       const rect = textBox.getBoundingClientRect();
       root.style.setProperty('--text-box-left', `${rect.left}px`);
       dotsPositionRaf = null;
     });
   }
   ```
   **Приоритет:** LOW (микро-оптимизация)

---

## Итог

| Вариант | Трудозатраты | Риски | Эффект | Оценка |
|---------|--------------|-------|--------|---------|
| **1. Лёгкие правки** | 15 мин | 🟢 Низкие | 90% | ✅ **ПРИМЕНИТЬ** |
| 2. Средние правки | 2-4 часа | 🔴 Высокие | 95% | ❌ Отклонить |
| 3. Изящные правки | 18-30 часов | 🔴 Критические | 100%* | ❌ Отклонить |

*при условии успешного рефакторинга и отсутствия регрессий

**Вердикт:** Применить Вариант 1 сейчас, запланировать полный рефакторинг на Q2 2025 после сбора метрик.
