# Новая логика админки

## Общая архитектура

Админка разделена на **3 основных раздела** по частоте использования:

1. **📚 Контент** — ежедневная работа с SEO, меню, paywall
2. **⚙️ Настройки** — цены, CTA, Robokassa, legal, расширенные настройки
3. **🚀 Сборка** — запуск билда, логи, редактор HTML-блоков

---

## Хардкод параметры (не выносятся в UI)

### robots (meta robots)

| Тип страницы | robots | Причина |
|--------------|--------|---------|
| article (free) | `index,follow` | Индексируем для SEO |
| article (premium) | `noindex,nofollow` | Закрытый контент |
| intro | `index,follow` | Главная точка входа |
| appendix | `noindex,nofollow` | Только для купивших |
| recommendation | `index,follow` | Посадочные страницы |
| legal | `noindex,nofollow` | Модалки, не страницы |

### canonical

| Тип страницы | canonical |
|--------------|-----------|
| article (free) | Автоматически: `/course/{slug}` |
| article (premium) | Указывает на free: `/course/{slug}` |
| intro | `/` |
| appendix | Сам на себя или не ставится |
| recommendation | Автоматически: `/recommendations/{slug}` |

### Прочие хардкод значения

- **og:type** — всегда `article` для контента, `website` для intro
- **twitter:card** — всегда `summary_large_image`
- **Язык** — `ru` (из конфига, не из UI)
- **Charset** — `UTF-8`

---

## Модель данных контента

### Единый JSON: `config/content-meta.json`

```json
{
  "course/01_раздел_1_жиры.md": {
    "type": "article",
    "slug": "zhiry-na-kuhne",
    "h1_md": "Жиры на кухне",
    "seo_h1": "Как убрать жир с кухни",
    "title": "Жиры на кухне — Слишком умная уборка",
    "meta_description": "Пошаговая инструкция...",
    "menu_label": "Жиры",
    "menu_subtitle": "Базовые принципы",
    "paywall": {
      "openBlocks": 3,
      "teaserBlocks": 2
    }
  },
  "recommendations/uborka-posle-remonta.md": {
    "type": "recommendation",
    "slug": "uborka-posle-remonta",
    "h1_md": "Уборка после ремонта",
    "seo_h1": "Как убрать квартиру после ремонта",
    "title": "Уборка после ремонта — Слишком умная уборка",
    "meta_description": "Как безопасно и без лишней химии убрать строительную пыль и следы ремонта.",
    "carousel_label": "После ремонта",
    "carousel_subtitle": "Строительная пыль и уборка",
    "carousel_icon": "🏠",
    "carousel_order": 1,
    "carousel_enabled": true
  }
}
```

### Поля по типам контента

| Поле             | article                                        | appendix                                       | intro                          | recommendation                                   | legal                          |
|------------------|------------------------------------------------|-----------------------------------------------|--------------------------------|--------------------------------------------------|--------------------------------|
| h1_md            | read-only                                      | read-only                                     | read-only                      | read-only                                        | read-only                      |
| slug             | ✅ опционально (если пусто → транслитерация)   | ✅ опционально (если пусто → транслитерация)  | ❌ (фиксированный route: intro) | ✅ опционально (если пусто → транслитерация)     | ❌ (фиксируется в роутинге)    |
| seo_h1           | ✅ обязат.                                     | опционально                                   | ❌ (=h1_md)                     | ✅                                              | ❌                             |
| title            | ✅                                            | ✅                                            | ✅                             | ✅                                              | ❌                             |
| meta_description | ✅                                            | ✅                                            | ✅                             | ✅                                              | ❌                             |
| menu_label       | ✅ fallback→h1                                 | ✅ fallback→h1                                | ❌                             | ❌                                              | ❌                             |
| menu_subtitle    | ✅ опцион.                                     | ✅ опцион.                                    | ❌                             | ❌                                              | ❌                             |
| paywall          | ✅                                            | ❌                                            | ❌                             | ❌                                              | ❌                             |
| carousel_label   | ❌                                            | ❌                                            | ❌                             | ✅ обязат.                                      | ❌                             |
| carousel_subtitle| ❌                                            | ❌                                            | ❌                             | ✅ обязат.                                      | ❌                             |
| carousel_icon    | ❌                                            | ❌                                            | ❌                             | опционально                                     | ❌                             |
| carousel_order   | ❌                                            | ❌                                            | ❌                             | опционально (по умолчанию в конец)              | ❌                             |
| carousel_enabled | ❌                                            | ❌                                            | ❌                             | ✅ (по умолчанию true)                          | ❌                             |

```text
slug (article/appendix/recommendation):
  - Если поле slug явно заполнено в админке → используем его при генерации URL:
    - /course/{slug} для статей,
    - /appendix/{slug} для приложений,
    - /recommendations/{slug} для рекомендаций.
  - Если поле slug пусто → slug вычисляется по h1_md по правилу транслитерации:
    - кириллица → латиница,
    - всё приводится к нижнему регистру,
    - пробелы и недопустимые символы заменяются на "-",
    - несколько подряд идущих "-" схлопываются в один.

seo_h1:
  - Если пусто → берём h1_md

title:
  - Если пусто → seo_h1 + " — " + title_suffix (из глобальных настроек)
  - Если seo_h1 тоже пусто → h1_md + " — " + title_suffix

menu_label (article/appendix):
  - Если пусто → h1_md

menu_subtitle (article/appendix):
  - Если пусто → не выводится (пустая строка)

carousel_label (recommendation):
  - Если пусто → h1_md

carousel_subtitle (recommendation):
  - Если пусто → не выводится (пустая строка)

meta_description:
  - Если пусто → global_meta_description (из глобальных настроек)
```

---


### Карусель рекомендаций (отдельно от меню курса)

- **Меню курса** (article/appendix) строится по полям `menu_label` / `menu_subtitle` и отвечает за основную навигацию по разделам и приложениям.
- **Рекомендации** — отдельный тип страниц (`type: "recommendation"`), которые используются как посадочные страницы и выводятся в виде карусели на сайте.

Для recommendation-постов в `content-meta.json` используются поля:

- `carousel_label` — заголовок карточки в карусели (крупный текст).
- `carousel_subtitle` — подпись под заголовком (малый текст).
- `carousel_icon` — emoji или путь до иконки (например, `/assets/recs/remont.png`).
- `carousel_order` — порядок сортировки в карусели (меньше число → левее/выше).
- `carousel_enabled` — флаг, показывать ли карточку в карусели.

Особенности:

1. Термин **«карусель»** используется только для recommendation. Для article/appendix в UI и коде по-прежнему используется термин **«меню»**.
2. Meta robots и canonical для recommendation **не редактируются в UI** и задаются по таблицам выше:
   - `robots: index,follow`
   - `canonical: /recommendations/{slug}`
3. В превью навигации в админке есть отдельная колонка «Рекомендации», которая визуально показывает, как будет выглядеть карусель (label + subtitle + иконка).
## Legal документы

### Логика

1. Билдер сканирует футер на наличие ссылок с атрибутом `data-legal`
2. Для каждой ссылки создаётся запись в админке
3. Администратор указывает, какой MD-файл соответствует какой ссылке

### Структура в футере (шаблон)

```html
<footer>
  <a href="#" data-legal="terms">Условия использования</a>
  <a href="#" data-legal="privacy">Политика конфиденциальности</a>
  <a href="#" data-legal="offer">Публичная оферта</a>
</footer>
```

### Конфиг legal: `config/legal.json`

```json
{
  "terms": {
    "label": "Условия использования",
    "file": "01-legal-terms.md",
    "exists": true
  },
  "privacy": {
    "label": "Политика конфиденциальности", 
    "file": "02-privacy.md",
    "exists": true
  },
  "offer": {
    "label": "Публичная оферта",
    "file": "03-offer.md",
    "exists": false
  }
}
```

### API

- `GET /api/legal` — возвращает список ссылок из футера + текущие привязки + статус файлов
- `POST /api/legal` — сохраняет привязки

---

## Paywall

### Визуализация

Редактор paywall должен наглядно показывать:

1. **Слайдер** — граница между открытой и закрытой частью
2. **Число блоков в блюре (teaser)** — сколько блоков показывать размытыми
3. **Превью** — реальный контент статьи, разделённый на:
   - Открытая часть (полностью видна)
   - Teaser (размыто)
   - Скрытая часть (не показывается)

### Структура paywall

```
┌─────────────────────────────────┐
│        ОТКРЫТАЯ ЧАСТЬ           │  ← openBlocks блоков
│   (полностью видна всем)        │
├─────────────────────────────────┤
│        TEASER (BLUR)            │  ← teaserBlocks блоков
│   (размыто, видны контуры)      │
├─────────────────────────────────┤
│        СКРЫТАЯ ЧАСТЬ            │  ← остальные блоки
│   (не рендерится на free)       │
└─────────────────────────────────┘
```

### Конфиг paywall (внутри content-meta.json)

```json
{
  "paywall": {
    "openBlocks": 3,
    "teaserBlocks": 2
  }
}
```

---

## Глобальные настройки

### Основные (в UI)

```json
{
  "pricing": {
    "originalAmount": 4990,
    "currentAmount": 2990,
    "currency": "RUB"
  },
  "ctaTexts": {
    "enterFull": "Получить полный доступ",
    "next": "Следующий раздел",
    "goToCourse": "Вернуться к курсу",
    "openCourse": "Начать курс"
  },
  "footer": {
    "companyName": "ИП Иванов И.И.",
    "inn": "123456789012",
    "year": 2025
  },
  "robokassa": {
    "merchantLogin": "...",
    "password1": "...",
    "password2": "...",
    "description": "Курс «Слишком умная уборка»",
    "isTest": true
  },
  "build": {
    "wordsPerMinute": 150
  },
  "seo": {
    "titleSuffix": "— Слишком умная уборка",
    "globalMetaDescription": "...",
    "globalOgImage": "/assets/og-default.jpg"
  }
}
```

### Расширенные (скрыты в аккордеоне)

```json
{
  "robokassa": {
    "invoicePrefix": "SUU",
    "successUrl": "/success.php",
    "failUrl": "/fail.php",
    "resultUrl": "/robokassa-callback.php",
    "culture": "ru"
  },
  "manifest": {
    "name": "Слишком умная уборка",
    "short_name": "СУУ",
    "theme_color": "#ffffff",
    "background_color": "#ffffff",
    "start_url": "/"
  },
  "features": {
    "cookiesBannerEnabled": true
  }
}
```


## Раздел «Сборка»

Раздел **🚀 Сборка** в админке отвечает не за контент, а за управление процессом генерации и предпросмотра сайта.

### Основные действия

1. **Запустить сборку**
   - Триггерит запуск продакшн-сборки статического сайта.
   - Использует текущие значения всех конфигов (`content-meta.json`, `legal.json`, глобальные настройки).
   - Результат: обновлённые HTML-страницы, карты сайта, мета-теги и т.д.
   - Конкретная CLI-команда (например, `npm run build`) задаётся в билдере и **не настраивается из UI**.

2. **Поднять локальный сервер**
   - Отдельная кнопка в том же блоке, что и запуск сборки.
   - Задача: запустить локальный dev-/preview-сервер для проверки результата сборки «как на проде», но на локальной машине.
   - Порт, команда и режим работы (dev/preview) определяются в backend/скриптах сборки. Админка только вызывает готовое действие.
   - Ожидаемое поведение: открыть сайт по `http://localhost:PORT` (порт задаётся на стороне сборщика).

### Логи и отладка

- В блоке «Сборка» остаётся просмотр логов последнего запуска (ошибки, предупреждения, длительность сборки).
- Логика логов не меняется: добавление кнопки «Поднять локальный сервер» **не влияет** на формат логов, только на набор доступных действий.

---

