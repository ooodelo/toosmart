# 📋 ПЛАН РЕАЛИЗАЦИИ СИСТЕМЫ

## 🎯 ЦЕЛЬ
Создать систему статичной генерации HTML из markdown-файлов с картинками + реализовать parallax scroll для рекомендаций.

---

## 📝 ТРЕБОВАНИЯ (от пользователя)

### 1. Markdown → HTML генерация
- **Входные данные:** `.md` файл с версткой и вставленными картинками
- **Процесс:** Генерация происходит **один раз** (статичная сборка)
- **Результат:** Готовые HTML страницы (один .md файл = одна HTML страница)
- **Картинки:** PNG файлы внутри текста как иллюстрации (не галереи, не увеличиваются)

### 2. Рекомендации (правая колонка)
- **НЕТ скролла** внутри блока рекомендаций
- **Несколько рекламных блоков** (2-4 штуки), которые просто висят на экране
- **Parallax эффект:**
  - Если высота рекомендаций > высота viewport → используем общий скролл
  - Блок рекомендаций скроллится **медленнее** текста (замедленный parallax)
  - Когда блок достигает конца → останавливается
  - Текст продолжает скроллиться дальше
  - При обратном скролле — та же механика в обратную сторону

### 3. Текстовый блок
- **НЕ разбиваем** на множество блоков
- Один markdown файл = один сплошной текст с заголовками
- Навигация "точками" генерируется из заголовков `## H2`

---

## 📦 СПИСОК ЗАДАЧ

### БЛОК 1: Подготовка инфраструктуры (1-2 часа)

**Задача 1.1: Установить зависимости**
- [ ] Создать `package.json`
- [ ] Установить `marked` (markdown парсер)
- [ ] (Опционально) Установить `live-server` для локальной разработки

**Команды:**
```bash
npm init -y
npm install marked
npm install --save-dev live-server
```

**Задача 1.2: Создать структуру папок**
```
/toosmart/
  /source/                    ← исходники
    /articles/                ← markdown файлы
      intro.md
      basics.md
    /images/                  ← все картинки
      intro-diagram.png
      basics-screenshot.png
    config.json              ← конфиг статей

  /public/                    ← результат сборки (готовый сайт)
    /articles/                ← сгенерированные HTML
    /images/                  ← скопированные картинки
    index.html
    styles.css
    script.js

  build.js                    ← скрипт генерации
  package.json
  index.html                  ← шаблон для генерации
```

**Задача 1.3: Создать config.json**
```json
{
  "articles": [
    {
      "id": "intro",
      "title": "Введение",
      "markdown": "intro.md",
      "next": "basics"
    },
    {
      "id": "basics",
      "title": "Основы",
      "markdown": "basics.md",
      "next": null
    }
  ]
}
```

---

### БЛОК 2: Build-скрипт (2-3 часа)

**Задача 2.1: Написать build.js**

Основные функции:
- [ ] `copyImages()` - копирование картинок из `source/images/` в `public/images/`
- [ ] `loadConfig()` - чтение `config.json`
- [ ] `parseMarkdown()` - конвертация .md → HTML
- [ ] `extractSections()` - извлечение заголовков H2 для навигации
- [ ] `generateArticleHTML()` - создание полной HTML страницы
- [ ] `updateIndex()` - обновление главной страницы с меню

**Ключевые моменты:**
```javascript
// Обработка изображений в markdown
renderer.image = (href, title, text) => {
  // Преобразуем относительные пути
  // ./image.png → /images/image.png

  return `
    <img src="/images/${imageName}"
         alt="${text}"
         loading="lazy">
  `;
};
```

**Задача 2.2: Настроить кнопку "Далее"**
- [ ] В build.js заменять плейсхолдер на реальную ссылку:
```javascript
if (article.next) {
  html = html.replace(
    'btn-next',
    `btn-next" onclick="location.href='${article.next}.html'`
  );
}
```

**Задача 2.3: Генерация меню статей**
- [ ] Автоматически создавать список статей в `.site-menu__list`
- [ ] Ссылки вида `/articles/intro.html`

---

### БЛОК 3: CSS стили для markdown (1 час)

**Задача 3.1: Добавить стили для markdown-элементов**

Нужно добавить в `styles.css`:

```css
/* Изображения из markdown */
.text-section img {
  max-width: 100%;
  height: auto;
  display: block;
  margin: 24px auto;
  border-radius: 8px;
}

/* Код блоки */
.text-section pre {
  background: #2d2d2d;
  color: #f8f8f2;
  padding: 16px;
  border-radius: 8px;
  overflow-x: auto;
}

.text-section code {
  background: var(--surface);
  padding: 2px 6px;
  border-radius: 4px;
  font-family: monospace;
}

/* Цитаты */
.text-section blockquote {
  border-left: 4px solid var(--border-color);
  padding-left: 16px;
  margin: 16px 0;
  color: #666;
}

/* Таблицы */
.text-section table {
  width: 100%;
  border-collapse: collapse;
  margin: 24px 0;
}

.text-section th,
.text-section td {
  padding: 12px;
  border: 1px solid var(--border-color);
}
```

---

### БЛОК 4: Parallax scroll для рекомендаций (2-3 часа)

**Задача 4.1: Изменить CSS для .stack**

В `styles.css` изменить:
```css
/* БЫЛО: */
.stack {
  position: sticky;
  top: var(--stack-top);
  max-height: calc(100dvh - ...);
}

.stack-list {
  overflow: auto; /* был внутренний скролл */
}

/* СТАНЕТ: */
.stack {
  position: relative; /* убрать sticky */
  /* убрать max-height */
}

.stack-list {
  overflow: visible; /* убрать скролл */
}
```

**Задача 4.2: Добавить JavaScript для parallax**

В `script.js` добавить функцию:

```javascript
function initParallaxStack() {
  const stack = document.querySelector('.stack');
  if (!stack) return;

  const PARALLAX_SPEED = 0.6; // коэффициент замедления

  function updateParallax() {
    // Только для desktop/tablet
    if (currentMode === 'handheld') {
      stack.style.transform = 'none';
      return;
    }

    const scrollY = window.scrollY;
    const stackHeight = stack.offsetHeight;
    const viewportHeight = window.innerHeight;

    // Parallax offset (медленнее основного скролла)
    let offset = -scrollY * (1 - PARALLAX_SPEED);

    // Ограничение: не выше начала
    offset = Math.min(0, offset);

    // Ограничение: остановка когда низ блока достиг низа viewport
    const maxOffset = -(stackHeight - viewportHeight);
    if (stackHeight > viewportHeight) {
      offset = Math.max(maxOffset, offset);
    }

    stack.style.transform = `translateY(${offset}px)`;
  }

  window.addEventListener('scroll', () => {
    requestAnimationFrame(updateParallax);
  }, { passive: true });

  updateParallax();
}
```

**Задача 4.3: Вызвать initParallaxStack() в init()**
```javascript
function init() {
  // ...существующий код...
  initParallaxStack(); // добавить эту строку
}
```

**Задача 4.4: Протестировать на разных экранах**
- [ ] Desktop (1920px): parallax работает плавно
- [ ] Tablet (1024px): parallax работает
- [ ] Handheld (<1024px): parallax отключен, обычное поведение

---

### БЛОК 5: Тестирование и настройка (1-2 часа)

**Задача 5.1: Создать тестовые markdown файлы**
- [ ] `intro.md` с 3-4 разделами H2
- [ ] `basics.md` с картинками
- [ ] Тестовые PNG изображения

**Задача 5.2: Запустить сборку**
```bash
npm run build
```

**Задача 5.3: Проверить результат**
- [ ] HTML файлы сгенерированы в `/public/articles/`
- [ ] Картинки скопированы в `/public/images/`
- [ ] Кнопка "Далее" ведет на следующую статью
- [ ] Меню слева содержит все статьи
- [ ] Навигация "точками" работает
- [ ] Parallax рекомендаций плавный

**Задача 5.4: Настроить коэффициент parallax**

Подобрать оптимальное значение `PARALLAX_SPEED`:
- `0.5` = в 2 раза медленнее
- `0.6` = в 2.5 раза медленнее (рекомендуется)
- `0.7` = в 3.3 раза медленнее

---

## 🔍 ДЕТАЛИ РЕАЛИЗАЦИИ

### Как работает генерация?

1. **Запуск:** `node build.js`
2. **Процесс:**
   - Читает `source/config.json`
   - Для каждой статьи:
     - Читает `.md` файл
     - Парсит markdown → HTML
     - Находит все `## H2` заголовки
     - Создает секции с ID для навигации
     - Обрабатывает пути к изображениям
     - Генерирует полный HTML из шаблона
     - Сохраняет в `/public/articles/{id}.html`
   - Копирует все изображения
   - Обновляет `index.html` с меню статей

3. **Результат:** Папка `/public/` готова к деплою

### Как работает parallax?

1. При скролле страницы вычисляется `scrollY`
2. Применяется коэффициент: `offset = -scrollY * (1 - PARALLAX_SPEED)`
3. Блок рекомендаций двигается медленнее через `transform: translateY()`
4. Когда низ блока достигает низа viewport → движение останавливается
5. Текст продолжает скроллиться

### Пример работы параллакса:

```
Скролл страницы: 0px    → Offset рекомендаций: 0px
Скролл страницы: 100px  → Offset рекомендаций: -40px (медленнее)
Скролл страницы: 500px  → Offset рекомендаций: -200px
...
Скролл страницы: 1000px → Offset рекомендаций: СТОП (достиг конца)
Скролл страницы: 1500px → Offset рекомендаций: СТОП (текст идет дальше)
```

---

## ⚙️ КОМАНДЫ

```bash
# Установка зависимостей
npm install

# Сборка статичного сайта
npm run build

# Локальный сервер для разработки
npm run dev

# Очистка результатов сборки
npm run clean
```

---

## 📁 ПРИМЕРЫ ФАЙЛОВ

### source/articles/intro.md
```markdown
# Введение

Текст статьи с **жирным** и *курсивом*.

![Диаграмма](intro-diagram.png "Подпись к изображению")

## Первый раздел

Контент первого раздела...

## Второй раздел

Контент второго раздела...
```

### source/config.json
```json
{
  "articles": [
    {
      "id": "intro",
      "title": "Введение",
      "markdown": "intro.md",
      "next": "basics"
    }
  ]
}
```

---

## 🎛️ НАСТРОЙКИ

### Коэффициент parallax
В `script.js`:
```javascript
const PARALLAX_SPEED = 0.6; // Чем меньше, тем медленнее
```

### Размер рекомендаций
В `styles.css`:
```css
--stack-width-desktop: clamp(320px, 28vw, 420px);
--stack-gap: 32px;
```

---

## ✅ КРИТЕРИИ ГОТОВНОСТИ

- [ ] Markdown конвертируется в HTML корректно
- [ ] Картинки отображаются в тексте
- [ ] Кнопка "Далее" переходит на следующую статью
- [ ] Меню слева содержит все статьи
- [ ] Навигация "точками" работает (desktop)
- [ ] Рекомендации НЕ имеют внутреннего скролла
- [ ] Parallax работает плавно (desktop/tablet)
- [ ] Блок рекомендаций останавливается в конце
- [ ] На handheld всё работает без parallax

---

## 📊 ОЦЕНКА ТРУДОЕМКОСТИ

| Блок | Задачи | Время |
|------|--------|-------|
| 1. Инфраструктура | package.json, структура папок | 1-2ч |
| 2. Build-скрипт | Парсинг, генерация HTML | 2-3ч |
| 3. CSS стили | Markdown элементы | 1ч |
| 4. Parallax scroll | JS + CSS правки | 2-3ч |
| 5. Тестирование | Проверка, настройка | 1-2ч |
| **ИТОГО** | | **7-11 часов** |

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

После реализации базовой версии можно добавить:
- [ ] Оптимизация изображений (sharp.js для webp)
- [ ] Подсветка синтаксиса кода (Prism.js)
- [ ] Lightbox для изображений (по клику)
- [ ] Индикатор прогресса чтения
- [ ] Темная тема
- [ ] Поиск по статьям

---

**Готово к обсуждению и уточнению!**
