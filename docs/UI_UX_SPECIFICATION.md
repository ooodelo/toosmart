# 📐 СПЕЦИФИКАЦИЯ UI/UX - ОБРАЗОВАТЕЛЬНАЯ ПЛАТФОРМА

**Дата:** 2025-11-15
**Версия:** 4.0 (актуальная)
**Статус:** Утверждено

---

## 🎯 ОБЩАЯ КОНЦЕПЦИЯ

Адаптивная образовательная платформа для чтения статей с четырьмя режимами отображения:
- **Mobile** (<768px) - вертикальная раскладка, dock внизу
- **Tablet** (768-899px) - вертикальная раскладка, меню слева
- **Desktop** (900-1279px) - полный функционал с dots-rail
- **Desktop-wide** (≥1280px) - увеличенная ширина текста, max-width контейнера

**Генерация контента:**
- Один markdown файл → одна HTML страница
- Статичная генерация (build один раз)
- Картинки PNG inline как иллюстрации

**Бизнес-модель: Freemium**
- Существует две версии сайта: **Free** и **Premium**
- Free версия: вступительные абзацы разделов + полные статьи "Рекомендации"
- Premium версия: полные разделы курса с навигацией

---

## 🆓 FREE VS PREMIUM ВЕРСИИ

### Концепция разделения

Платформа имеет **две параллельные версии** с разным уровнем доступа к контенту:

| Элемент | Free версия | Premium версия |
|---------|-------------|----------------|
| **URL** | `/free/` | `/premium/` |
| **Разделы курса** | Только вступительные абзацы | Полные разделы |
| **Статьи "Рекомендации"** | ✅ Полностью доступны | ✅ Полностью доступны |
| **Навигация (меню)** | ✅ Видна полная структура | ✅ Видна полная структура |
| **Flyout меню** | ✅ Подразделы H2 видны | ✅ Подразделы H2 видны |
| **Dots-rail** | ✅ Работает | ✅ Работает |
| **Кнопка внизу раздела** | 🔒 "Получить полную версию" | ➡️ "Далее" |
| **Доступ** | Открытый | Требует авторизации |
| **SEO индексация** | Только статьи "Рекомендации" | ❌ Закрыто от поисковиков |

### Free версия - Paywall UI

**Структура раздела курса в free версии:**

```html
<section id="grease-removal" class="text-section">
    <h2>Удаление жира</h2>

    <!-- ВСТУПИТЕЛЬНЫЙ АБЗАЦ (видимый) -->
    <p>Жир — один из самых стойких загрязнений на кухне...</p>

    <!-- БЛЮР ЭФФЕКТ (остальной контент) -->
    <div class="premium-teaser">
        <div class="blurred-content">
            <p>Для эффективного удаления жира нужны...</p>
            <p>Затем происходит процесс эмульгирования...</p>
            <!-- ... весь остальной контент под блюром ... -->
        </div>

        <!-- ОВЕРЛЕЙ С КНОПКОЙ -->
        <div class="unlock-overlay">
            <button class="btn-unlock" onclick="openPaymentModal()">
                🔒 Получить полную версию
            </button>
        </div>
    </div>
</section>
```

**CSS для paywall:**

```css
.premium-teaser {
    position: relative;
    margin-top: 24px;
}

.blurred-content {
    filter: blur(8px);
    pointer-events: none;
    user-select: none;
    max-height: 400px;
    overflow: hidden;
    -webkit-mask-image: linear-gradient(to bottom, black 50%, transparent 100%);
    mask-image: linear-gradient(to bottom, black 50%, transparent 100%);
}

.unlock-overlay {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    z-index: 10;
}

.btn-unlock {
    padding: 16px 32px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border: none;
    border-radius: 12px;
    font-size: 18px;
    font-weight: 600;
    cursor: pointer;
    box-shadow: 0 8px 24px rgba(102, 126, 234, 0.4);
    transition: transform 0.2s, box-shadow 0.2s;
}

.btn-unlock:hover {
    transform: translateY(-2px);
    box-shadow: 0 12px 32px rgba(102, 126, 234, 0.5);
}
```

### Модальное окно оплаты

**Визуальная структура:**

```html
<div class="modal" id="payment-modal" hidden>
    <div class="modal-overlay" onclick="closePaymentModal()"></div>

    <div class="modal-content">
        <button class="modal-close" onclick="closePaymentModal()">×</button>

        <h2>Получите полный доступ к курсу</h2>

        <ul class="benefits">
            <li>✅ 10 полных разделов с подробными объяснениями</li>
            <li>✅ Практические рецепты и таблицы совместимости</li>
            <li>✅ Пожизненный доступ к материалам</li>
            <li>✅ Обновления курса бесплатно</li>
        </ul>

        <p class="price">
            <span class="price-old">1990 ₽</span>
            <span class="price-current">990 ₽</span>
        </p>

        <form action="https://auth.robokassa.ru/Merchant/Index.aspx" method="GET">
            <input type="email" name="Shp_email" placeholder="Ваш email" required>
            <button type="submit" class="btn-pay">Оплатить 990 ₽</button>
        </form>

        <p class="security-note">🔒 Безопасная оплата через Robokassa</p>
    </div>
</div>
```

**CSS для модального окна:**

```css
.modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 2000;
    display: flex;
    align-items: center;
    justify-content: center;
}

.modal-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.7);
    backdrop-filter: blur(4px);
}

.modal-content {
    position: relative;
    background: white;
    border-radius: 16px;
    padding: 40px;
    max-width: 500px;
    width: 90%;
    max-height: 90vh;
    overflow-y: auto;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
    z-index: 1;
}

.modal-close {
    position: absolute;
    top: 16px;
    right: 16px;
    background: none;
    border: none;
    font-size: 32px;
    cursor: pointer;
    color: #666;
    line-height: 1;
}

.benefits {
    list-style: none;
    padding: 0;
    margin: 24px 0;
}

.benefits li {
    padding: 8px 0;
    font-size: 16px;
}

.price {
    text-align: center;
    margin: 24px 0;
}

.price-old {
    text-decoration: line-through;
    color: #999;
    font-size: 20px;
    margin-right: 12px;
}

.price-current {
    font-size: 32px;
    font-weight: 700;
    color: #667eea;
}

.btn-pay {
    width: 100%;
    padding: 16px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border: none;
    border-radius: 12px;
    font-size: 18px;
    font-weight: 600;
    cursor: pointer;
    margin-top: 16px;
}

.security-note {
    text-align: center;
    color: #666;
    font-size: 14px;
    margin-top: 16px;
}
```

### Premium версия - Форма входа

**server/index.php - UI авторизации:**

```html
<div class="auth-page">
    <div class="auth-container">
        <img src="../free/assets/CleanLogo.svg" alt="Clean" class="auth-logo">

        <h1>Вход в закрытую версию курса</h1>
        <p>Введите данные, отправленные на email после оплаты</p>

        <form action="auth.php" method="POST" class="auth-form">
            <input type="email" name="email" placeholder="Email" required autofocus>
            <input type="password" name="password" placeholder="Пароль из письма" required>
            <button type="submit">Войти в курс</button>
        </form>

        <p class="error" hidden>❌ Неверный email или пароль</p>

        <hr>

        <p class="help-text">
            Еще нет доступа? <a href="/free/">Вернуться к бесплатной версии</a><br>
            Проблемы со входом? <a href="mailto:support@toosmart.com">Напишите нам</a>
        </p>
    </div>
</div>
```

**CSS для формы входа:**

```css
.auth-page {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.auth-container {
    background: white;
    padding: 48px;
    border-radius: 16px;
    max-width: 440px;
    width: 90%;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
}

.auth-logo {
    display: block;
    margin: 0 auto 24px;
    max-width: 200px;
}

.auth-form input {
    width: 100%;
    padding: 14px;
    margin-bottom: 16px;
    border: 2px solid #e0e0e0;
    border-radius: 8px;
    font-size: 16px;
    transition: border-color 0.2s;
}

.auth-form input:focus {
    border-color: #667eea;
    outline: none;
}

.auth-form button {
    width: 100%;
    padding: 16px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border: none;
    border-radius: 8px;
    font-size: 18px;
    font-weight: 600;
    cursor: pointer;
}

.help-text {
    text-align: center;
    color: #666;
    font-size: 14px;
    margin-top: 24px;
}
```

### Статьи "Рекомендации"

**Назначение:**
- Всегда **полностью бесплатные**
- Доступны по адресу `/free/articles/`
- **Индексируются Google** для привлечения трафика
- В конце каждой статьи - кнопка перехода к курсу

**Структура статьи:**

```html
<article class="text-box">
    <h1>Переплата за «эко»-средства</h1>

    <!-- ПОЛНЫЙ КОНТЕНТ БЕЗ ОГРАНИЧЕНИЙ -->
    <p>Разбираем состав экологичных средств и их реальную стоимость...</p>
    <!-- ... весь контент ... -->

    <!-- ПРИЗЫВ К ДЕЙСТВИЮ -->
    <div class="article-cta">
        <h3>Хотите узнать больше?</h3>
        <p>Изучите полный курс «Clean - Теория правильной уборки» с 10 разделами по химии уборки</p>
        <a href="/free/index.html" class="btn-course">
            Перейти к полному курсу →
        </a>
    </div>
</article>
```

**CSS для CTA:**

```css
.article-cta {
    background: linear-gradient(135deg, #e8f4f8 0%, #d4e9f2 100%);
    border: 2px solid #667eea;
    border-radius: 12px;
    padding: 32px;
    margin-top: 48px;
    text-align: center;
}

.btn-course {
    display: inline-block;
    padding: 14px 32px;
    background: #667eea;
    color: white;
    text-decoration: none;
    border-radius: 8px;
    font-weight: 600;
    margin-top: 16px;
    transition: transform 0.2s;
}

.btn-course:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(102, 126, 234, 0.4);
}
```

---

## 📄 СТРУКТУРА САЙТА

### Free версия (/free/):
```
/free/
  index.html                  ← Раздел "Введение" (вступление + paywall)
  kitchen.html                ← Раздел "Кухня" (вступление + paywall)
  bathroom.html               ← Раздел "Ванная" (вступление + paywall)
  ...                         ← остальные разделы курса

  articles/                   ← Статьи "Рекомендации" (ПОЛНЫЕ, SEO)
    eco-myths.html            ← "Переплата за эко-средства"
    dangerous-mix.html        ← "Опасные комбинации"
    soda-vinegar.html         ← "Сода + уксус не работает"
    ph-neutral-myth.html      ← "pH-нейтрально — маркетинг"
```

### Premium версия (/premium/):
```
/premium/
  index.php                   ← Форма входа (email + password)
  auth.php                    ← Проверка учетных данных
  logout.php                  ← Выход из аккаунта

  home.html                   ← Главная после входа (защищена)
  kitchen.html                ← Раздел "Кухня" (ПОЛНЫЙ, защищен)
  bathroom.html               ← Раздел "Ванная" (ПОЛНЫЙ, защищен)
  ...                         ← остальные разделы (ПОЛНЫЕ, защищены)
```

### Внутри раздела курса (free):
- H1 заголовок раздела
- Вступительный абзац (видимый)
- Остальной контент под блюром
- Кнопка "🔒 Получить полную версию" → модальное окно

### Внутри раздела курса (premium):
- H1 заголовок раздела
- Полный контент без ограничений
- Несколько подразделов с H2 заголовками
- Текст, изображения, таблицы, код, цитаты
- Кнопка "Далее →" в конце (переход к следующему разделу)

### Внутри статьи "Рекомендации":
- H1 заголовок статьи
- Полный контент
- CTA блок с кнопкой "Перейти к полному курсу"

---

## 1️⃣ ТЕКСТОВЫЙ БЛОК (центральная колонка)

### Назначение:
Основной контент статьи

### Структура:
- **Один файл = одна страница**
- Внутри: несколько разделов (H2)
- Сплошной поток контента (не разбивается на блоки)

### Поведение:
- ✅ Обычный вертикальный скролл
- ✅ Изображения PNG inline (не галереи, не zoom)
- ✅ Таблицы, код, цитаты внутри потока

### Ширина (индустриальный стандарт Habr):
- **Mobile** (<768px): 100% контейнера (с отступами 30px)
- **Tablet** (768-899px): 520px
- **Desktop** (900-1279px): 580px
- **Desktop-wide** (≥1280px): 660px с max-width контейнера 1400px

### Отступы на мобильных:
- Горизонтальные отступы: 30px (16px от .main + 14px от .panel)
- Оптимизировано для узких экранов 300-320px

### Markdown элементы:
- `<img>` - центрированные, border-radius, max-width: 100%
- `<pre><code>` - блоки кода с подсветкой фона, overflow-x: auto на mobile
- `<code>` - inline код
- `<blockquote>` - цитаты с левым бордером
- `<table>` - таблицы с границами, overflow-x: auto на mobile
- `<ul>`, `<ol>` - списки
- `<a>` - ссылки
- `<hr>` - горизонтальные линии
- H3, H4 - подзаголовки

---

## 2️⃣ НАВИГАЦИЯ "ТОЧКАМИ" (dots-rail)

### Назначение:
Быстрая навигация **внутри** текущей статьи

### Расположение:
Слева от текста, вертикальный ряд точек

### Генерация:
- **Автоматически** из H2 заголовков
- 5 разделов H2 → 5 точек
- Обновляется при переходе на другую статью

### Поведение:
- ✅ Клик по точке → скролл к разделу **внутри страницы**
- ✅ Активная точка подсвечивается (текущий раздел)
- ✅ IntersectionObserver отслеживает видимость разделов

### Режимы:
- **Desktop** (900-1279px): ✅ Видны
- **Desktop-wide** (≥1280px): ✅ Видны
- **Tablet** (768-899px): ❌ Скрыты
- **Mobile** (<768px): ❌ Скрыты

### CSS:
- `position: fixed`
- Desktop: `left: calc(50vw - 580px/2 - 48px)`
- Desktop-wide: `left: calc(50vw - 660px/2 - 48px)`
- `z-index: 150`

---

## 3️⃣ КНОПКА НАВИГАЦИИ (внизу раздела)

### Назначение:

**В Premium версии:** Переход на **следующий раздел** курса
**В Free версии:** Открытие **модального окна оплаты**
**В статьях "Рекомендации":** Переход к **курсу**

### Визуальное поведение (только Premium):

**Во время скролла:**
- ✅ `position: sticky`
- ✅ Прилипает к низу экрана (`bottom: 24px`)
- ✅ Видна всегда при скролле

**Когда скролл закончен:**
- ✅ Встает в обычную позицию в конце текста
- ✅ `position: static` срабатывает когда достигнут конец

### Логика клика:

**Premium версия - переход к следующему разделу:**

```javascript
function handleNext() {
  const nextPageUrl = btnNext?.dataset.nextPage;
  if (nextPageUrl) {
    window.location.href = nextPageUrl; // переход на kitchen.html, bathroom.html и т.д.
  }
}
```

**Free версия - открытие модального окна:**

```javascript
function handlePaywall() {
  openPaymentModal(); // показать модальное окно с формой оплаты
}
```

**Статья "Рекомендации" - переход к курсу:**

```javascript
function handleCourseCTA() {
  window.location.href = '/free/index.html'; // переход к началу курса
}
```

### HTML примеры:

**Premium версия:**
```html
<button class="btn-next" type="button" data-next-page="bathroom.html">
  Далее →
</button>
```

**Free версия:**
```html
<button class="btn-unlock" type="button" onclick="openPaymentModal()">
  🔒 Получить полную версию
</button>
```

**Статья "Рекомендации":**
```html
<a href="/free/index.html" class="btn-course">
  Перейти к полному курсу →
</a>
```

### Режимы (только для btn-next в Premium):
- Desktop: sticky, центрирована
- Desktop-wide: sticky, центрирована
- Tablet: sticky, центрирована
- Mobile: обычная кнопка в конце, ширина min(320px, 100%)

**Примечание:** Кнопка paywall (free) и CTA (статьи) НЕ используют sticky позиционирование.

---

## 4️⃣ ОГЛАВЛЕНИЕ (левое меню, menu-rail)

### Назначение:
Навигация по всем статьям сайта

### Заголовок:
**"Структура курса"**

### Структура меню:
```
Структура курса
├─ 1. Введение в курс
│  ├─ Что вы узнаете         ← H2
│  ├─ Как это работает       ← H2
│  └─ Приступим              ← H2
├─ 2. Основы химического состава
│  ├─ Что такое pH           ← H2
│  ├─ Типы компонентов       ← H2
│  └─ ...
└─ 3. ПАВы и их виды
   └─ ...
```

**Показываем:**
- ✅ Название статьи (жирный шрифт, на отдельной карточке)
- ✅ H2 заголовки под каждой статьей (список с отступом)

### Генерация:
Автоматически из конфига:

```json
{
  "articles": [
    {
      "id": "intro",
      "title": "Введение в курс",
      "sections": [
        {"id": "section-1", "title": "Что вы узнаете"},
        {"id": "section-2", "title": "Как это работает"}
      ]
    }
  ]
}
```

### Поведение:
- ✅ Клик по названию статьи → загрузка страницы
- ✅ Клик по H2 → загрузка страницы + скролл к разделу
- ✅ Независимый скролл внутри меню
- ✅ **Когда меню открыто → скролл текста НЕ работает**

### Открытие/закрытие:

**Desktop:**
- Hover по ярлыку → временный выезд (`is-slid`)
- Клик по ярлыку → полное открытие (`menu-open`)
- Клик по бэкдропу / Escape → закрыть

**Tablet:**
- Tap по ярлыку → открыть
- **Свайп слева направо** от левого края (50px зона) → открыть
- **Свайп справа налево** при открытом меню → закрыть
- Tap по бэкдропу / Escape → закрыть

**Mobile:**
- Меню снизу (плашка-док с текстом "Структура курса")
- Tap по ярлыку → открыть
- **Свайп снизу вверх** от нижнего края (80px зона) → открыть
- **Свайп сверху вниз** от cap или верхних 120px меню → закрыть
- Tap по cap / backdrop / Escape → закрыть

### Свайпы (оптимизированы для лучшего UX):
**Mobile (вертикальные свайпы):**
- Открытие: минимум 60px от нижних 80px экрана
- Закрытие: минимум 80px от cap или верхних 120px меню
- Порог определения направления: 15px
- Не конфликтует со скроллом контента меню

**Tablet (горизонтальные свайпы):**
- Открытие: минимум 60px от левых 50px экрана
- Закрытие: минимум 80px справа налево
- Порог определения направления: 15px

### Позиционирование:
- Desktop/Tablet: слева, фиксировано
- Mobile: снизу, на всю ширину

### Размеры:
- Закрыто: 70px (только ярлык)
- Открыто: 320px

---

## 5️⃣ РЕКОМЕНДАЦИИ (правая колонка, stack)

### Назначение:
Рекламные/сопутствующие материалы

### Заголовок:
**"🔥 Рекомендуем"**

### Количество карточек:
2-5 карточек

### Пример карточек:
```html
<li class="stack-card">
  <h3>ВЫ НЕ УМЕЕТЕ ЧИТАТЬ ЭТИКЕТКИ</h3>
  <p>Гид по химическому составу для тех, кто хочет жить</p>
</li>
```

### КАРУСЕЛЬ (НОВОЕ В v3.0):

**Автоматическая ротация:**
- ✅ Карусель автоматически переключает слайды каждые **6 секунд**
- ✅ Плавная смена через opacity transition (0.5s)
- ✅ Пауза при наведении мыши (desktop)
- ✅ Индикатор прогресса (точки внизу)
- ✅ Клик по точкам для ручного переключения

**Свайпы на тач-устройствах:**
- ✅ Свайп влево → следующий слайд
- ✅ Свайп вправо → предыдущий слайд
- ✅ Минимальная дистанция: 50px
- ✅ Умное определение направления (предотвращает вертикальный скролл)
- ✅ Порог направления: 10px
- ✅ Автоматическая ротация перезапускается после свайпа

**Компактный режим:**
- Desktop (900-1000px): уменьшенные шрифты для узкого sidebar

### Режимы:
- **Desktop** (900-1279px): справа, sticky, карусель с автосменой
- **Desktop-wide** (≥1280px): справа, sticky, карусель, max-width 400px
- **Tablet** (768-899px): под текстом, карусель с автосменой
- **Mobile** (<768px): под текстом, карусель с автосменой и свайпами

### CSS:
```css
.stack {
  position: sticky;
  top: var(--stack-top);
  padding: 20px;
  overflow: hidden;
}

.stack-carousel {
  position: relative;
  overflow: hidden;
}

.stack-slide {
  opacity: 0;
  visibility: hidden;
  transition: opacity 0.5s ease;
}

.stack-slide[data-active="true"] {
  opacity: 1;
  visibility: visible;
}
```

### JavaScript:
```javascript
// Автоматическая смена каждые 6 секунд
const SLIDE_INTERVAL = 6000;

// Свайпы с предотвращением скролла
stack.addEventListener('touchmove', (e) => {
  if (swipeDirection === 'horizontal') {
    e.preventDefault(); // не скроллим страницу
  }
}, { passive: false });
```

---

## 6️⃣ HEADER (шапка)

### Содержимое:
- Логотип (слева)
- Emoji "🤚" (декоративный элемент)
- Ссылки: "Помощь", "О курсе" (справа)

### Поведение:

**Desktop/Desktop-wide:**
- `position: sticky`
- `top: 0`
- **Всегда видим** (автоскрытие НЕ работает)
- При открытии меню → сдвигается синхронно с меню

**Tablet/Mobile (НОВОЕ В v3.0):**
- `position: sticky`
- `top: 0`
- **Автоскрытие при скролле:**
  - Скролл вниз (>100px) → `transform: translateY(-100%)` - **скрывается**
  - Скролл вверх → `transform: translateY(0)` - **появляется**
  - В самом верху (≤100px) → всегда видим
- Плавная анимация: `transition: transform 300ms ease`

### Размеры:
- Высота: динамическая (padding: 12px)
- Mobile/Tablet: padding-left/right: 16px (уменьшено с 32px)
- Desktop: padding: 12px 32px

### Технические детали автоскрытия:
```javascript
// Минимальная дистанция скролла: 10px
// Порог "верха страницы": 100px
// Контролируется через body[data-scroll="down"|"up"]
```

---

## 7️⃣ DOCK (плашка внизу, только Mobile)

### Назначение:
Кнопка открытия меню на мобильных устройствах

### Текст:
**"Структура курса"** (было "Меню")

### Поведение:
- ✅ **Всегда видим** (НЕ скрывается при скролле)
- ✅ `position: fixed; bottom: 0;`
- ✅ Tap → открывает меню снизу
- ✅ Учитывает safe-area-inset для notch устройств

### Режимы:
- Mobile: ✅ Виден
- Tablet/Desktop: ❌ Скрыт (display: none)

### Z-index:
- `z-index: 1600` (выше всего, кроме backdrop при открытом меню)

---

## 📐 РЕЖИМЫ АДАПТИВНОСТИ

### 1. Mobile (<768px)

**Детекция:**
- Ширина <768px

**Раскладка:**
- Flex-direction: column
- Порядок: header → текст → кнопка "Далее" → рекомендации → dock

**Элементы:**
- ✅ Header с автоскрытием при скролле
- ✅ Dock внизу (всегда видим)
- ✅ Меню снизу с вертикальными свайпами
- ✅ Карусель рекомендаций с свайпами
- ❌ Точек нет

**Особенности:**
- Отступы: 30px (16+14)
- Текст адаптивный: 100% ширины
- Overflow-x: auto для таблиц и кода

---

### 2. Tablet (768-899px)

**Детекция:**
- 768px ≤ ширина < 900px

**Раскладка:**
- Flex-direction: column
- Текст: max-width 520px, центрирован
- Порядок: header → текст → кнопка "Далее" → рекомендации

**Элементы:**
- ✅ Header с автоскрытием при скролле
- ✅ Меню слева с горизонтальными свайпами
- ✅ Карусель рекомендаций
- ❌ Точек нет
- ❌ Dock не показывается

**Edge gesture:**
- Клик по левому краю (30px зона) → открывает меню

---

### 3. Desktop (900-1279px)

**Детекция:**
- 900px ≤ ширина < 1280px
- НЕ touch устройство (или touch на широком экране)

**Раскладка:**
- Grid: `1fr | 580px | 1fr`
- Рекомендации в 3-й колонке справа

**Элементы:**
- ✅ Header всегда видим (без автоскрытия)
- ✅ Dots-rail для навигации внутри статьи
- ✅ Карусель рекомендаций
- ✅ Меню слева (overlay) с hover/click

**Компактный режим (900-1000px):**
- Уменьшенные шрифты в карточках рекомендаций

---

### 4. Desktop-wide (≥1280px)

**Детекция:**
- Ширина ≥1280px
- НЕ touch устройство

**Раскладка:**
- Grid: `1fr | 660px | 1fr`
- Container: max-width 1400px, margin: 0 auto
- Предотвращает растягивание на ultra-wide мониторах

**Элементы:**
- ✅ Header всегда видим
- ✅ Dots-rail (работает с centered container)
- ✅ Карусель рекомендаций, max-width 400px
- ✅ Меню слева (overlay)

**Особенности:**
- Увеличенная ширина текста: 660px
- Центрированный контейнер для ultra-wide экранов
- Sidebar ограничен 400px

---

## 🎨 ВИЗУАЛЬНЫЕ ПРАВИЛА

### Цветовая схема (обновлено в v3.0):
```css
:root {
  --surface: #f6f6f6;
  --surface-alt: #ededed;
  --text-color: #222;
  --text-secondary: #666;
  --border-color: #d0d0d0;
  --code-inline-bg: var(--surface);
  --code-inline-color: #c7254e;
  --code-block-bg: #2d2d2d;
  --code-block-color: #f8f8f2;
  --link-color: #0066cc;
  --link-hover: #004499;
}

body {
  background: #e8f4f8; /* Светло-голубой фон */
}
```

### Скругления:
- Все карточки, блоки: `border-radius: 12px`
- Кнопки: `border-radius: 12px`
- Изображения: `border-radius: 8px`
- Dots: `border-radius: 50%` (12px × 12px)

### Границы:
- Все блоки: `border: 1px solid var(--border-color)`

### Отступы:
- `--gap-page: 32px` (desktop/tablet)
- Mobile: 16px (main) + 14px (panel/stack) = 30px итого
- `--stack-gap: 32px` (desktop/tablet), 20px (mobile)
- Секции: `margin-bottom: 32px`
- Изображения: `margin: 24px auto`

### Переходы и анимации:
- Header hide: `300ms cubic-bezier(0.4, 0, 0.2, 1)`
- Карусель fade: `0.5s ease`
- Меню rail: `220ms cubic-bezier(0.4, 0, 0.2, 1)`
- Dots активация: `120ms ease`

### Шрифты:
- Семейство: 'Inter', 'Segoe UI', sans-serif
- Основной текст: 16px, line-height: 1.6
- H2: крупнее, font-weight: 600
- Code: 'Consolas', 'Monaco', monospace, 0.9em (inline)

---

## 🔄 ВЗАИМОДЕЙСТВИЯ

### Сценарий 1: Чтение статьи на Desktop

1. Пользователь на странице `intro.html`
2. Скроллит вниз:
   - Текст двигается вверх
   - Рекомендации автоматически меняются каждые 6 секунд
   - Dots-rail показывает текущий раздел
   - Кнопка "Далее" прилипла к низу
3. Кликает по точке → быстрый скролл к разделу
4. Дочитал до конца → кнопка "Далее" в обычной позиции
5. Клик "Далее" → переход на `basics.html`

### Сценарий 2: Чтение статьи на Mobile

1. Открывает статью
2. Скроллит вниз:
   - Header **скрывается** (больше места для контента)
   - Dock остается видимым внизу
   - Карусель рекомендаций автоматически меняется
3. Скроллит вверх:
   - Header **появляется** обратно
4. Свайпает карусель влево → следующая рекомендация
5. Свайп снизу вверх → открывает меню "Структура курса"
6. Свайп сверху вниз от cap → закрывает меню

### Сценарий 3: Навигация через меню на Tablet

1. Свайп слева направо от края → меню открывается
2. Скролл основного контента блокируется
3. Скроллит внутри меню
4. Кликает "Основы → Типы компонентов" → переход на `basics.html#section-2`
5. Свайп справа налево → меню закрывается

### Сценарий 4: Карусель с автосменой

1. Открывает статью с 3 рекомендациями
2. Видит слайд 1 (точка 1 активна)
3. Через 6 секунд → автоматическая смена на слайд 2
4. Наводит мышь (desktop) → автосмена останавливается
5. Кликает точку 3 → мгновенный переход
6. Убирает мышь → автосмена возобновляется с текущего слайда

---

## ⚙️ ТЕХНИЧЕСКИЕ ДЕТАЛИ

### data-mode:
```javascript
body.dataset.mode = 'mobile' | 'tablet' | 'desktop' | 'desktop-wide';
```

### data-input:
```javascript
body.dataset.input = 'touch' | 'pointer';
```

### data-scroll (новое в v3.0):
```javascript
body.dataset.scroll = 'down' | 'up';
// Контролирует автоскрытие header на mobile/tablet
```

### Состояния меню:
- `body.menu-open` - меню открыто
- `body.is-slid` - временный выезд (hover на desktop)
- `body[data-lock="scroll"]` - скролл заблокирован

### Z-index порядок:
- Контент: 0
- dots-rail: 150
- Header: 200
- Backdrop: 1400
- menu-rail: 1500
- Dock (mobile): 1600

### Breakpoints (индустриальный стандарт):
- Mobile: < 768px (iPad portrait)
- Tablet: 768-899px
- Desktop: 900-1279px (Every Layout рекомендация)
- Desktop-wide: ≥ 1280px (стандартные ноутбуки)

---

## 📝 ПРАВИЛА ГЕНЕРАЦИИ

### Из markdown в HTML:

**Входные данные:**
```markdown
# Заголовок статьи

![Картинка](image.png)

## Раздел 1

Текст с **жирным** и *курсивом*.

```javascript
code block
```

| Table | Header |
|-------|--------|
| Data  | Data   |
```

**Генерация:**
1. Парсинг markdown → HTML (marked.js)
2. Извлечение H2 заголовков → создание секций с ID
3. Обработка путей изображений (`./image.png` → `/images/image.png`)
4. Создание навигации (dots-rail)
5. Генерация меню (из конфига)
6. Установка `data-next-page` для кнопки "Далее"

### Конфигурация:
```json
{
  "articles": [
    {
      "id": "intro",
      "title": "Введение",
      "markdown": "intro.md",
      "next": "basics"
    }
  ]
}
```

---

## ✅ КЛЮЧЕВЫЕ ИЗМЕНЕНИЯ v3.0

| Элемент | v2.0 | v3.0 (актуально) |
|---------|------|------------------|
| Режимы | 3 режима (desktop/tablet-wide/handheld) | **4 режима** (mobile/tablet/desktop/desktop-wide) |
| Breakpoints | 1024px, 1440px | **768px, 900px, 1280px** (Habr стандарт) |
| Ширина текста | 680px/540px | **500→520→580→660px** (плавный рост) |
| Max-width | Нет | **1400px** для desktop-wide (ultra-wide защита) |
| Рекомендации | Static list | **Карусель с автосменой** (6 сек) |
| Свайпы карусели | Нет | ✅ Влево/вправо, предотвращение скролла |
| Свайпы меню | Нет | ✅ **Вертикальные** (mobile), **горизонтальные** (tablet) |
| Header скрытие | Нет | ✅ **Автоскрытие** на mobile/tablet при скролле |
| Dock текст | "Меню" | **"Структура курса"** |
| Фон | #f6f4f1 | **#e8f4f8** (светло-голубой) |
| Mobile отступы | 64px (32+32) | **30px (16+14)** оптимизировано |
| Dots-rail | Только desktop | **Desktop + desktop-wide** |

---

## 🎯 ИТОГОВЫЕ ТРЕБОВАНИЯ

### Обязательные:
1. ✅ Четыре режима адаптивности (mobile/tablet/desktop/desktop-wide)
2. ✅ Индустриальные breakpoints: 768px, 900px, 1280px
3. ✅ Плавный рост ширины текста: 500→520→580→660px
4. ✅ Карусель рекомендаций с автосменой каждые 6 секунд
5. ✅ Свайпы для карусели (touch devices)
6. ✅ Свайпы для меню (вертикальные mobile, горизонтальные tablet)
7. ✅ Автоскрытие header при скролле (mobile/tablet)
8. ✅ Умное определение направления свайпов (предотвращение скролла)
9. ✅ Кнопка "Далее": sticky визуально, переход между страницами
10. ✅ Меню: показывает статьи + их H2 заголовки
11. ✅ Dots-rail: desktop и desktop-wide режимы
12. ✅ Блокировка скролла при открытом меню
13. ✅ Max-width 1400px для desktop-wide (защита от ultra-wide)

### Запрещено:
- ❌ Внутренний скролл в рекомендациях (кроме мобильного overflow)
- ❌ Кнопка "Далее" скроллит внутри статьи
- ❌ Zoom/lightbox для изображений
- ❌ Галереи изображений
- ❌ Блокировка нормального вертикального скролла при свайпах

---

## 🔧 ОПТИМИЗАЦИИ ПРОИЗВОДИТЕЛЬНОСТИ

### RAF использование:
- Скролл обработка (header hide)
- Resize обработка
- Параллакс анимации (если используется)

### Passive listeners:
- `scroll` события: `{ passive: true }`
- `touchstart`: `{ passive: true }`
- `touchend`: `{ passive: true }`
- `touchmove`: `{ passive: false }` только когда нужен preventDefault

### Throttling:
- Скролл: минимум 10px для срабатывания
- Свайпы: минимум 10-15px для определения направления
- Карусель: 6000ms между автосменами

---

**Конец спецификации v3.0**
