# 📊 Таблица интеграции: Где настраивается что

## Полный справочник всех точек настройки Legal документов

---

## 1️⃣ СОЗДАНИЕ И РЕДАКТИРОВАНИЕ КОНТЕНТА

| Что | Где | Как | Кто |
|---|---|---|---|
| **Условия использования** | `/content/legal/01-legal-terms.md` | Отредактировать через текстовый редактор | Вы |
| **Политика конфиденциальности** | `/content/legal/02-privacy-policy.md` | Отредактировать через текстовый редактор | Вы |
| **Публичная оферта** | `/content/legal/03-public-offer.md` | Отредактировать через текстовый редактор | Вы |
| **Альтернативная оферта** | `/content/legal/offer.md` | Отредактировать через текстовый редактор | Вы |
| **Пользовательское документ** | `/content/legal/04-название.md` | Создать новый файл и отредактировать | Вы |

**Что менять в этих файлах:**
- `example.com` → ваш домен
- `support@example.com` → ваша почта
- `privacy@example.com` → почта для конфиденциальности
- `+7 (XXX) XXX-XX-XX` → ваш номер телефона
- `2024` → текущий год
- `[Укажите название компании]` → ваше юридическое название
- `[Укажите ИНН]` → ваш ИНН
- `[Укажите адрес]` → ваш юридический адрес

---

## 2️⃣ КОНФИГУРАЦИЯ В АДМИН-ПАНЕЛИ

| Что | Где | URL | Как | Результат |
|---|---|---|---|---|
| **Форма Legal документы** | `/admin/index.html` | `http://localhost:3001` | Открыть админ-панель (`npm run admin`) | Отображение формы |
| **Поле: Файл условий/оферты** | `/admin/index.html:663` | `localhost:3001` | Ввести имя: `01-legal-terms.md` | Связь условий с конфигом |
| **Поле: Политика конфиденциальности** | `/admin/index.html:668` | `localhost:3001` | Ввести имя: `02-privacy-policy.md` | Связь политики с конфигом |
| **Поле: Публичная оферта** | `/admin/index.html:672` | `localhost:3001` | Ввести имя: `03-public-offer.md` | Связь оферты с конфигом |
| **Кнопка "Сохранить конфигурацию"** | `/admin/index.html:680` | `localhost:3001` | Нажать кнопку | POST к `/api/config` |

**Админ-панель показывает:**
- Подсказку: "Имя файла в content/legal/" (указывает на нужную папку)
- Плейсхолдеры с примерами: `01-legal-terms.md`, `02-privacy.md`, `03-offer.md`
- Статус сохранения через Toast уведомления

---

## 3️⃣ ОБРАБОТКА ЗАПРОСА НА СЕРВЕРЕ

| Что | Где | Функция | Строка | Что делает |
|---|---|---|---|---|
| **GET конфига** | `/admin/server.js` | `handleGetConfig()` | 104-115 | Читает `/config/site.json` и отправляет браузеру |
| **POST конфига** | `/admin/server.js` | `handleSaveConfig()` | 117-155 | Получает данные из формы, валидирует, сохраняет в JSON |
| **Валидация** | `/admin/server.js` | `validateConfig()` | 158-176 | Проверяет корректность цены, ИНН и т.д. |
| **Создание бэкапа** | `/admin/server.js` | `handleSaveConfig()` | 137-141 | Создает резервную копию: `/config/site.json.backup` |
| **Логирование** | `/admin/server.js` | `handleSaveConfig()` | 146 | Логирует: `[timestamp] Конфигурация сохранена` |

**Сервер обрабатывает:**
- CORS запросы (для локального использования)
- Парсит JSON из тела запроса
- Валидирует данные перед сохранением
- Создает автоматические бэкапы

---

## 4️⃣ ХРАНЕНИЕ КОНФИГУРАЦИИ

| Что | Где | Формат | Пример |
|---|---|---|---|
| **Основной конфиг** | `/config/site.json` | JSON | `{"legal": {"terms": "01-legal-terms.md", ...}}` |
| **Резервная копия** | `/config/site.json.backup` | JSON | Автоматически создается перед сохранением |
| **Версионирование** | Git | `.gitignore` исключает или включает | Можно отслеживать изменения конфига |

**Структура legal секции в site.json:**
```json
{
  "legal": {
    "terms": "01-legal-terms.md",
    "privacy": "02-privacy-policy.md",
    "offer": "03-public-offer.md"
  }
}
```

---

## 5️⃣ ОБРАБОТКА ПРИ СБОРКЕ

| Что | Где | Процесс | Результат |
|---|---|---|---|
| **Запуск сборки** | `/admin/index.html` | Кнопка "🔄 Собрать всё" | POST к `/api/build` |
| **Выбор target** | `/admin/server.js:194-207` | Определение команды (`--target=free\|premium\|all`) | `node scripts/build.js --target=all` |
| **Выполнение** | `/scripts/build.js` | `execSync()` с таймаутом 60 сек | Скрипт запускается и обрабатывает legal файлы |
| **Чтение конфига** | `/scripts/build.js` | Импорт `/config/site.json` | `config.legal.terms`, `config.legal.privacy`, etc. |
| **Проверка файлов** | `/scripts/build.js` | `fs.existsSync()` для каждого файла | Ошибка если файл не найден |
| **Преобразование** | `/scripts/build.js` | Markdown-to-HTML конвертер | HTML версия legal документов |
| **Встраивание** | `/scripts/build.js` | Подстановка в шаблоны | HTML встраивается в `/dist/` файлы |
| **Логирование** | `/admin/server.js:210,220` | Логирование начала и завершения | `[timestamp] Сборка завершена успешно` |

**Переменные окружения:**
- `NODE_ENV` — окружение
- `TARGET` — какую версию собирать (free/premium/recommendations/all)
- `PORT` (в админ-панели) — порт сервера (обычно 3001)

---

## 6️⃣ РЕЗУЛЬТАТЫ В dist/

| Что | Где | Формат | Содержит |
|---|---|---|---|
| **Free версия** | `/dist/index.html` | HTML | Весь контент free версии + legal документы |
| **Premium версия** | `/dist/premium/index.html` | HTML | Весь контент premium версии + legal документы |
| **Рекомендации** | `/dist/recommendations/index.html` | HTML | Рекомендации + legal документы |
| **Карта сайта** | `/dist/sitemap.xml` | XML | Может включать ссылки на legal |
| **Assets** | `/dist/css/`, `/dist/js/` | CSS, JS | Стили и скрипты для всех страниц |

**Что в dist/ о legal:**
1. Meta-теги (OpenGraph, description и т.д.)
2. Ссылки в футере на legal документы
3. Полные тексты legal документов (если включены)
4. JavaScript для навигации по legal страницам

---

## 7️⃣ ВКЛЮЧЕНИЕ/ОТКЛЮЧЕНИЕ

| Что | Как включить | Как отключить |
|---|---|---|
| **Документ в админ-панели** | Указать имя файла и сохранить | Очистить поле и сохранить |
| **Legal документы на сайте** | Собрать проект после сохранения конфига | Удалить имя файла из конфига и пересобрать |
| **Конкретный документ** | Указать имя существующего файла | Указать имя несуществующего файла или удалить |
| **Все legal документы** | Все три поля заполнены | Все три поля пусты |

---

## 8️⃣ ИНСТРУМЕНТЫ И КОМАНДЫ

| Что | Команда | Описание |
|---|---|---|
| **Запуск админ-панели** | `npm run admin` | Запустить сервер на localhost:3001 |
| **Сборка через админ-панель** | Нажать кнопку в UI | Эквивалент npm run admin:build |
| **Сборка через terminal** | `npm run admin:build` | Собрать все версии без админ-панели |
| **Сборка Free версии** | `node scripts/build.js --target=free` | Только бесплатный курс |
| **Сборка Premium версии** | `node scripts/build.js --target=premium` | Только платный курс |
| **Редактировать файл** | `nano /content/legal/01-legal-terms.md` | Открыть в nano редакторе |
| **Проверить конфиг** | `cat /config/site.json \| jq .legal` | Показать только legal секцию |
| **Список файлов** | `ls -la /content/legal/*.md` | Показать все markdown файлы |

---

## 9️⃣ ТИПИЧНЫЙ WORKFLOW

### Сценарий: Добавить новый legal документ

```
1. Создать файл:
   nano /content/legal/04-refund-policy.md
   (написать содержимое)

2. Открыть админ-панель:
   npm run admin
   (откроется localhost:3001)

3. Пролистать в конце страницы до "Legal документы"
   (должна быть карточка с тремя полями)

4. ❌ ВАЖНО: текущая админ-панель поддерживает ТОЛЬКО 3 документа!
   Нужно отредактировать /admin/index.html чтобы добавить 4-е поле

5. После добавления 4-го поля в админ-панель:
   - Указать: "04-refund-policy.md"
   - Нажать: "💾 Сохранить конфигурацию"
   - Нажать: "🔄 Собрать всё"

6. Проверить результат:
   - Открыть /dist/index.html в браузере
   - Проверить что ссылка на новый документ работает
```

---

## 🔟 ВЛАДЕЛЬЦЫ И ОТВЕТСТВЕННОСТЬ

| Элемент | За что отвечает | Кто может менять | Когда менять |
|---|---|---|---|
| **Содержимое файлов** | Контент legal документов | Юрист / Контент-менеджер | Когда меняется политика/условия |
| **Имена файлов в конфиге** | Какие файлы использовать | Администратор сайта | Когда добавляется новый документ |
| **HTML админ-панели** | Форма и UI | Frontend разработчик | Когда нужно добавить поле для 4-го документа |
| **Сервер админ-панели** | Обработка запросов | Backend разработчик | Когда нужна функция загрузки файлов |
| **Скрипт сборки** | Интеграция в финальный HTML | Frontend разработчик | Когда меняется способ подключения legal |
| **Конфиг site.json** | Какие значения использовать | Администратор сайта | Когда меняются параметры (домен, тексты и т.д.) |

---

## 1️⃣1️⃣ ОШИБКИ И КАК ИХ ИСПРАВИТЬ

| Ошибка | Причина | Где искать | Как исправить |
|---|---|---|---|
| "Файл не найден при сборке" | Имя в конфиге не совпадает с имени файла | `/config/site.json` vs `/content/legal/` | Проверить совпадение имен и расширений |
| "Legal документы не видны на сайте" | Конфиг не сохранен или файла не существует | `/config/site.json` и `/content/legal/` | Проверить что файл существует, сохранить конфиг, пересобрать |
| "Админ-панель не обновляется" | Кеш браузера | Браузер | Ctrl+Shift+Del (очистить кеш) или F5 (пересоздать) |
| "Cannot read property 'legal'" | Поле удалено из конфига | `/config/site.json` | Добавить "legal": {} в конфиг вручную |
| "ENOENT: no such file or directory" | Файл физически не существует | `/content/legal/` | Создать файл вручную |
| "Сохранение не работает" | Ошибка валидации (ИНН, цена и т.д.) | Логи в админ-панели | Прочитать ошибку и исправить данные |

---

## 1️⃣2️⃣ ТОЧКИ РАСШИРЕНИЯ (для разработчика)

Если нужна дополнительная функциональность:

| Что расширить | Где менять | Что добавить | Сложность |
|---|---|---|---|
| **Добавить 4-й документ** | `/admin/index.html` (строки 659-674) | Новое поле input + обработка в JS | ⭐ Низкая |
| **Загрузка файлов** | `/admin/server.js` (строки 242-247) | multipart/form-data парсер + fs.writeFile | ⭐⭐ Средняя |
| **Редактирование содержимого** | `/admin/index.html` + `/admin/server.js` | Textarea + endpoint для сохранения | ⭐⭐ Средняя |
| **Версионирование документов** | `/scripts/build.js` | Git интеграция или архивирование | ⭐⭐⭐ Высокая |
| **Автоматическая проверка** | Новый скрипт или /scripts/build.js | Проверка ссылок, опечаток, HTML валидность | ⭐⭐⭐ Высокая |
| **Мультиязычность** | Везде | Разные папки или префиксы файлов | ⭐⭐⭐ Высокая |

---

## Итоговая таблица: Как работает каждая часть

```
┌─────────────────────┬──────────────────┬─────────────────┬──────────────────┐
│  ЭТАП               │  ФАЙЛ/ПАПКА      │  КТО            │  ДЕЙСТВИЕ        │
├─────────────────────┼──────────────────┼─────────────────┼──────────────────┤
│ 1. Редактирование   │ /content/legal/  │ Юрист/Вы       │ Писать/Менять    │
│ 2. Конфигурирование │ /admin/          │ Администратор  │ Указать имена    │
│ 3. Сохранение       │ /config/site.json│ Сервер админ   │ POST /api/config │
│ 4. Сборка           │ /scripts/build.js│ Node.js        │ Обработать       │
│ 5. Результат        │ /dist/           │ Браузер        │ Показать юзеру   │
└─────────────────────┴──────────────────┴─────────────────┴──────────────────┘
```

